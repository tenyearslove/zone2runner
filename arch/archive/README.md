# arch/archive — 탈락/대체된 아키텍처 결정 (역사 기록)

현재 시스템에 **적용되지 않는** ADR을 모아둔 폴더다. "무엇을 검토했고 왜 접었는가"는 설계 능력의 증거이므로 삭제하지 않고 보존한다. 내용은 이동 시점 그대로다(본문 속 문서 경로는 이동 전 기준일 수 있음).

| 문서 | 결정이었던 것 | 왜 탈락했나 | 대체 |
|---|---|---|---|
| adr-003 | Zone2 판정 = 규칙+개인화+MLP 하이브리드 | 판정 MLP 결함(라벨 순환)으로 판정 경로 재설계. 개인화/규칙 원칙만 살아남음 | adr-013/016 |
| adr-005 | Zone2 판정 MLP 채택 | 시뮬 라벨 자기참조 지름길 학습 결함 발견 | adr-013 (규칙 판정) |
| adr-011 | 온디바이스 MLP 순전파 런타임(순수 Kotlin) | 앱 내 NN이 전부 제거되어(판정 MLP/역치 NN/심박 NN) 적용 대상 소멸 | adr-020 (예측=ODE, 모델 파일 없음) |
| adr-014 | NN 역치 추정으로 개인화 | 개인화는 온라인 Bayesian이 맞는 도구(소수/온라인 라벨/콜드스타트/불확실성) | adr-016 |
| adr-018 | 음성/호흡 분석으로 토크테스트 객관화 | 온디바이스 음향 분석 신뢰 불가(실검증: YAMNet이 날숨을 오분류 등) → 주관 5단계 설문 유지 | spec-016 (5단계 토크테스트) |
| adr-019 | NN 예측의 온라인 개인 보정(LMS) | 보정 대상 NN 폐기. 온라인 점추정 원리는 ODE 파라미터 추정으로 승계 | adr-020 |
| adr-022 | 워치 존 = 폰 경계 미러(워치 재계산) | 판정/색 규칙이 두 곳에 남아 폰-워치 불일치 재발 → 워치를 무로직 뷰어로 전환 | adr-023 |
| adr-020 | 심박 예측 = 생리 ODE + 온라인 개인화 | 예측 참값 부재로 정확도 검증이 짜고치기, 코칭 지연 허용, 경계 정확도 우선 → 예측 자체를 드롭 | adr-024 (관측 분석 엔진) |
| hr-prediction-formula-and-evidence | 예측 공식 유래/근거 정본 | adr-020 예측 드롭에 종속 소멸 | adr-024 |
| hr-prediction-multiscale-trend-design | 다중 시간대 추이 예측 설계안(미승인) | 예측 재설계 탐색안, 예측 드롭으로 무효 | adr-024 |
| hr-prediction-쉬운설명 | 위 설계안의 쉬운 설명판 | 상동 | adr-024 |

현행 결정은 `arch/`, 탈락 명세는 `spec/archive/`, 탈락 보고서는 `report/archive/` 참조.
