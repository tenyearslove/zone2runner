# Zone2Runner 백서 — 바닥부터 상위까지, 완전한 이해를 위한 문서

> 이 문서 하나만 처음부터 끝까지 읽으면, **배경지식이 전혀 없어도** 이 앱이 무엇을 왜 어떻게
> 하는지 완전히 이해할 수 있도록 쓴 백서다. 운동생리학과 AI(머신러닝) 기초부터 시작해서,
> 우리가 내린 설계 결정과 실제 구현, 그리고 정직한 한계까지 아우른다.
>
> 최종 갱신: 2026-07-06 (예측 보정 검증 -53% + 가상러너 강화 + 경사 거리창 + 프롬프트 외부화 +
> **토크테스트 단측 관측 정정 + 기온 코칭 + 프로필 관리/개인화 시각화 + AI 설명(QA6) + ML 정비**).
> 학습용 상세는 `STUDY_GUIDE.md`, 발표용은 `report/report-003`, 설계 결정 종합은 `report/report-004`.

---

## 목차

- **Part 0. 90초 요약** — 바쁘면 이것만
- **Part 1. 배경지식 (아무것도 몰라도 되는 출발점)**
  - 1-1 심박수로 운동 강도를 아는 원리
  - 1-2 Zone 2란 무엇인가 (유산소 대사의 과학)
  - 1-3 왜 사람마다 Zone 2가 다르고, 왜 정확히 알 수 없는가
  - 1-4 Zone 2를 추정하는 방법들 (그리고 각자의 함정)
  - 1-5 심박은 "늦게" 반응한다 — HR 동역학
- **Part 2. AI/머신러닝 기초 (비유로 쉽게)**
  - 2-1 머신러닝이 뭔지, 신경망이 뭔지, Bayesian이 뭔지, 규칙이 뭔지
  - 2-2 핵심 철학: AI ≠ 신경망, 문제마다 맞는 도구
- **Part 3. 우리가 푸는 문제와 설계 여정**
  - 3-1 문제 정의
  - 3-2 실패에서 배운 설계 (판정 신경망의 몰락)
- **Part 4. 시스템 전체 그림**
  - 4-1 기기 배치 (워치 + 폰)
  - 4-2 1초에 무슨 일이 일어나나 (파이프라인)
- **Part 5. AI 컴포넌트 4개, 바닥부터**
  - 5-1 심박 예측 (DP1: 어떤 NN 아키텍처를 쓸지)
  - 5-2 개인 Zone 2 학습 (DP2: 도구 선택 — 온라인 Bayesian)
  - 5-3 Zone 2 판정 (DP3: 도구 선택 — 규칙)
  - 5-4 코칭 문장 (DP4: 도구 선택 — 규칙 방향 + LLM 표현)
  - 5-5 콜드스타트: 프로필로 첫 추정
- **Part 6. 검증 — 가상러너와 폐루프**
- **Part 7. 정직한 한계**
- **부록** — 용어사전 / 파일 지도 / 참고문헌

---

## Part 0. 90초 요약

이 앱은 **Zone 2**(유산소 기초 운동 강도)를 개인 맞춤으로 코칭한다. 핵심 난제는 **"Zone 2는 사람마다
다른데, 랩(병원) 검사 없이는 정확히 알 수 없다"**는 것이다. 우리는 이걸 **네 가지 도구**로 나눠 푼다.

| 하는 일 | 도구 | 왜 이 도구? |
|---|---|---|
| **곧(30/60초 뒤) 심박이 어떻게 될지 예측** | **신경망(딥러닝)** | 심박은 연속으로 쏟아지는 대량 데이터 → 신경망이 맞는 자리 |
| **이 사람의 Zone 2 범위를 실주행으로 학습** | **온라인 Bayesian** | 정답(말하기 테스트)이 소수/온라인 → 신경망은 과적합, Bayesian이 맞음 |
| **지금 Zone 2 안/밖 판정** | **규칙** | 경계와 비교만 하면 됨 → 학습 불필요, 규칙이 안전 |
| **코칭 문장 만들기** | **LLM(생성형)** | 자연스러운 표현 |

여기에 더해, 심박 예측 신경망은 "평균 러너"로 학습된 고정 모델이라, **뛰는 도중 그 사람에게 맞춰
오차를 실시간으로 배워 보정**하는 다섯 번째 조각(온라인 개인 보정)을 얹었다. 이것도 머신러닝이다.

> **용어 오해 방지**: "온라인 학습"의 **online은 인터넷과 무관하다.** 데이터가 실시간으로 하나씩
> 도착할 때마다 즉석에서 조금씩 배운다는 뜻이다. 이 앱은 전부 **폰 안에서(오프라인, 비행기 모드에서도)**
> 돈다 — 네트워크를 쓰지 않는다.

한 줄로: **"AI는 신경망만이 아니다. 문제의 데이터 특성에 맞춰 도구를 골랐다."** 이게 이 프로젝트의
설계 철학이자, "왜 여기엔 신경망을 쓰고 저기엔 안 썼는가"에 답할 수 있는 근거다.

---

# Part 1. 배경지식 (아무것도 몰라도 되는 출발점)

## 1-1. 심박수로 운동 강도를 아는 원리

운동을 하면 근육이 산소를 더 쓴다. 산소를 나르는 건 혈액이고, 혈액을 미는 건 심장이다. **더 세게
운동할수록 심장은 더 빨리 뛴다.** 그래서 심박수(분당 박동, bpm)는 "지금 얼마나 힘든가"의 좋은 대리
지표다. 값싼 손목 시계로 잴 수 있어서 대중 운동 앱이 다 심박을 쓴다.

두 개의 기준점이 중요하다.
- **안정시 심박(RHR, Resting Heart Rate)**: 가만히 쉴 때의 심박. 체력이 좋을수록 낮다(훈련된 사람 40~55,
  일반인 60~80). 심장이 한 번에 더 많은 피를 밀어서 덜 뛰어도 되기 때문.
- **최대심박(HRmax)**: 죽을힘을 다해도 더는 안 올라가는 심박의 천장. 나이가 들수록 낮아진다.
  전력질주로 실측하는 게 정확하고, 공식(예: 208 − 0.7×나이)은 어디까지나 평균 추정.

이 둘 사이 폭을 **여유심박(HRR, Heart Rate Reserve) = HRmax − RHR** 이라 부른다. "내가 쓸 수 있는
심박 범위"다.

## 1-2. Zone 2란 무엇인가 (유산소 대사의 과학)

운동 강도를 보통 5구간(Zone 1~5)으로 나눈다. **Zone 2는 두 번째, "편하게 오래 할 수 있는 유산소
강도"**다. 그런데 표면적 정의를 넘어서, Zone 2는 **1차 젖산역치(LT1, first Lactate Threshold) 바로
아래**를 가리키는 생리학 개념이다.

- **젖산(lactate)**: 근육이 에너지를 만들 때 나오는 부산물. 낮은 강도에선 생성과 제거가 균형을 이뤄
  혈중 젖산이 안정(~1 mmol/L) 수준에 머문다. 강도를 올리면 어느 지점부터 젖산이 처음으로 **의미
  있게 오르기 시작**한다 — 그 지점이 **LT1**(대략 2 mmol/L 부근)이다.
- **왜 LT1 바로 아래가 중요한가**: 이 강도에서 **미토콘드리아 호흡, 지방 산화(fat oxidation),
  모세혈관 동원**이 최대가 된다. 더 세게 하면 이 유산소 적응 이득은 오히려 줄고 피로 비용만 커진다.
  즉 Zone 2는 "지구력의 엔진(미토콘드리아)을 키우는" 가장 효율적인 강도다.
- 현대 Zone 2 개념은 **San Millán & Brooks(2018)**의 대사 연구(프로 사이클 선수의 미토콘드리아 밀도가
  경기력과 직결)와 **Seiler**의 양극화 훈련(엘리트는 훈련의 ~80%를 이 낮은 강도에서 보냄) 분석에서
  정립됐다. 그래서 Zone 2는 "적당히 편한 유산소"가 아니라 **LT1이라는 개인 생리 경계에 고정된 강도**다.

> 감각적으로는 **"대화가 가능하지만 노래는 못 할" 정도**가 Zone 2다. 이 "대화 가능성"이 뒤에 나올
> **토크 테스트**의 근거가 된다.

## 1-3. 왜 사람마다 Zone 2가 다르고, 왜 정확히 알 수 없는가

LT1의 위치는 **사람마다 크게 다르다.** 같은 40세라도 마라토너의 LT1은 최대심박의 75~80%에,
운동을 안 한 사람은 60~65%에 있을 수 있다. 체력, 훈련 이력, 유전이 다 영향을 준다. **그래서 "40세는
Zone 2가 몇 bpm" 같은 일괄 공식은 개인에게 안 맞는다.**

그런데 더 근본적인 벽이 있다. **LT1의 참값(진짜 정답)을 재려면 둘 중 하나가 필요하다:**
- **혈중 젖산 검사**: 점점 세게 뛰며 손가락을 반복 채혈해 젖산 곡선의 꺾임을 찾는다(랩/침습적).
- **가스 교환 검사**: 호흡 가스 분석기로 환기 변곡(VT1)을 찾는다(랩 장비).

**손목 심박과 폰 GPS만으로는 둘 다 불가능하다.** 즉 우리를 포함한 모든 소비자 Zone 2 도구는
**참값을 모르는 채 대리지표로 추정**할 수밖에 없다. 이건 우리 구현의 한계가 아니라 **문제 자체의
성질**이고, 우리는 이걸 제약 **C04(참값 부재)**로 명시한다.

이 사실이 프로젝트 전체를 규정한다. **정답표가 없으니**:
- "판정이 정답과 맞는가"로 성능을 못 잰다 → 대신 "현재 심박 대비 코칭 방향이 맞는가"로 평가한다.
- 완벽히 못 맞히더라도 **공식보다 낫게, 그리고 그 사람의 실제 반응(말하기 테스트)에 맞춰** 좁혀가는 게 목표.

```
   운동 강도(→) 올라감
   │
   │  Zone 1        Zone 2 (여기!)      Zone 3~5
   │  아주 편함   ┌──────────────┐    힘듦 ~ 최대
   │             │  유산소 기초    │
   │  ───────────┤  대화 가능     ├──── LT1(참값=랩만 앎) ────► 젖산 급증
   │             └──────────────┘
   │             하한(~60%HRmax)  상한(~70%HRmax≈LT1)
```

## 1-4. Zone 2를 추정하는 방법들 (그리고 각자의 함정)

랩이 없을 때 쓰는 대리지표들. 각자 장단이 있다.

| 방법 | 원리 | 함정 |
|---|---|---|
| **%HRmax (60~70%)** | 최대심박의 비율 | 체력(안정심박)을 무시 |
| **%HRR / Karvonen** | 여유심박 비율 + 안정심박 더하기 | **여유심박이 큰 사람에게 과대평가**(아래 참고) |
| **토크 테스트** | 편하게 말할 수 있는 마지막 강도 ≈ VT1/LT1 | 자기보고(주관) |
| **HR-속도 디커플링** | 같은 페이스인데 심박이 오르기 시작(드리프트) | 임계보다 높게 잡히는 편향(Conconi) |
| **DFA-α1 (심박변이도)** | 심박 변동의 프랙탈 지수가 0.75를 지나는 지점이 LT1 | RR간격 신호 품질/계산 부담 |

**우리가 겪은 실제 함정 (중요)**: 처음엔 %HRR(Karvonen)을 기준으로 썼다. 그런데 최대심박을 **실측한**
사용자(예: 최대 200, 안정 60)에게 "%HRR 70%"는 계산해보면 **최대심박의 79%**가 된다 — 이건 유산소
Zone 2가 아니라 힘든 Zone 3~4다. 여유심박(140)이 큰 사람일수록 %HRR이 안정심박을 다시 더하는 방식
때문에 유독 높게 나온다. 실제로 그 사용자가 **이미 적절한 유산소 강도로 뛰고 있는데도** 경계가 부풀려진
탓에 앱은 "미달"이라며 계속 더 빨리 뛰라고 몰아세웠다 — 그대로 따르면 오히려 진짜 Zone 2를 넘겨버린다.

→ **그래서 기준을 %HRmax로 재보정했다**(2026-07-04). Zone 2 상단 = **최대심박의 70%**(LT1 근사).
이게 감각(대화 가능한 강도)과 문헌(San Millán/Seiler의 Zone 2)과 맞는다. (자세한 계산은 5-2/5-5)

## 1-5. 심박은 "늦게" 반응한다 — HR 동역학

운동 강도를 갑자기 올려도 **심박은 즉시 안 따라온다.** 서서히, 대략 **지수함수적으로(시간상수 τ)**
목표치를 향해 올라간다(운동생리학의 mono-exponential HR on-kinetics). 오르막에 막 들어서면 힘은 이미
넘쳤는데 심박은 20~40초 뒤에야 반영된다.

또 하나, **카디악 드리프트(Cardiac Drift)**: 같은 페이스로 오래 뛰면 땀으로 혈액량이 줄고 체온이 올라
심장이 더 자주 뛰어야 한다 → 페이스는 그대로인데 심박이 서서히 오른다(주로 혈역학적 원인).

이 두 성질이 왜 중요한가:
- **심박이 늦게 반응**하니, "지금 심박"만 보고 코칭하면 항상 늦다. **"이 페이스면 곧 심박이 어디까지
  갈까"를 예측**하면 넘기 전에 미리 코칭할 수 있다 → 이게 **심박 예측이 필요한 이유**이자 DP1의 전제다
  (예측에 NN을 쓴다는 것까지가 전제, 그 NN을 어떤 아키텍처로 구현하느냐가 DP1의 실제 결정 — 5-1).
- **드리프트**는 "이 강도를 얼마나 버티나"의 신호라 개인 경계 추정의 보조 관측이 된다.

**왜 예측이 반응형보다 유리한가 (구체적으로)**: HR on-kinetics의 시간상수 τ는 대략 20~40초다. 즉
페이스를 올리면 심박이 새 정상상태에 도달하기까지 **1분 안팎**이 걸린다. 여기서 반응형(지금 심박만
보는) 코칭의 구조적 한계 세 가지가 나온다.
1. **경보가 늦다(지연 오차)**: 오르막에 진입해 부하가 이미 Zone 2를 넘었어도, 심박은 20~40초 뒤에야
   경계를 넘는다. 반응형은 그 시점에야 "초과"를 외치는데, **그 순간 사용자는 이미 목표보다 30~60초를
   초과 강도로 달린 뒤**다. 예측형은 "이 페이스를 유지하면 40초 뒤 상단을 넘는다"를 미리 알려 **넘기
   전에** 페이스를 늦추게 한다.
2. **과잉교정(오버슈트)이 는다**: 반응형 신호에 맞춰 사람이 페이스를 바꿔도, 그 결과가 심박에 나타나기까지
   또 τ만큼 걸린다. 그래서 "느낌상 아직 안 내려갔네" 하고 더 줄이다 저존으로 떨어지고, 다시 올리다 초과하는
   **진자 운동**이 생긴다. 심박의 지연을 모델이 먼저 감안해 코칭하면 이 왕복이 줄어 **Zone 2 체류 시간이 는다**
   (Zone 2 훈련의 가치는 곧 체류 시간이다).
3. **드리프트를 선반영한다**: 같은 페이스라도 후반에는 카디악 드리프트로 심박이 서서히 오른다. 예측 모델은
   경과 시간(elapsed_min)을 입력으로 받아 **"페이스가 그대로여도 곧 오른다"**를 반영하므로, 후반에 미리
   살짝 늦추라는 코칭이 가능하다. 반응형은 드리프트가 경계를 넘은 뒤에야 반응한다.

한 줄로: **심박이 τ만큼 늦으니, 코칭도 딱 그만큼 앞서야 제때 닿는다.** 예측은 그 리드타임을 만들어 내는
장치이고, 30/60초 두 지평(horizon)을 쓰는 이유도 이 τ 스케일에 맞춘 것이다(자세한 아키텍처는 5-1).

---

# Part 2. AI/머신러닝 기초 (비유로 쉽게)

## 2-1. 네 가지 도구, 아주 쉽게

**머신러닝(ML)** = 데이터를 보고 모델 속 숫자를 조금씩 고쳐 점점 잘 맞히게 만드는 것.
비유: 활쏘기 — 왼쪽으로 빗나가면 조준을 오른쪽으로 조금 튼다. 수백 번 하면 과녁에 맞는다.
**중요: "학습(ML)"은 신경망만의 것이 아니다.** 데이터로 숫자를 고치면 다 ML이다.

- **신경망(NN, 딥러닝)**: 숫자들을 여러 층에서 "곱하고 더하고 꺾기(비선형)"를 반복해 복잡한 관계를
  근사하는 함수. **데이터가 아주 많아야** 제대로 학습된다(수천~수만 개). 연속 다신호 비선형 문제에 강함.
- **Bayesian(베이지안)**: "처음엔 대충 추측하고, 증거가 들어올 때마다 추측을 그쪽으로 조금 옮기며,
  얼마나 확신하는지(불확실성)까지 들고 다니는" 방법. **데이터가 적어도** 되고, 온라인으로 하나씩
  들어오는 증거에 강함. 신경망이 아니지만 엄연히 ML(데이터로 숫자를 고침).
  - 비유: 새 동네 출근시간 — 첫날 "대충 30분?" → 한 번 다녀보니 34분, 추측을 살짝 옮김 → 여러 번
    다니면 실제값에 수렴하고 확신이 커짐. 어쩌다 사고로 60분 걸려도 크게 안 흔들림(이미 확신이 쌓여서).
- **규칙(Rule)**: 학습 없이 "if 이러면 저렇게" 하는 결정론적 논리. 학습할 게 없는 문제에 맞음.
- **LLM(대형 언어 모델)**: 문장을 생성하는 생성형 AI. 우리는 코칭 문장 "표현"에만 쓴다(남의 모델).

## 2-2. 핵심 철학: AI ≠ 신경망, 문제마다 맞는 도구

흔한 오해가 "AI = 신경망(딥러닝)"이다. 그래서 아무 데나 신경망을 붙이면, **문제와 안 맞는 곳에 써서
결함이 난다.** 우리도 그 실수를 했고(Part 3-2), 교정하며 배운 원칙이 이거다:

> **데이터가 연속 다수면 신경망, 소수 온라인 라벨이면 Bayesian, 결정론이면 규칙, 표현이면 LLM.**
> "왜 여기엔 신경망을 안 썼나"에 근거를 대는 것이 곧 설계 능력이다.

```
             데이터가 많고 연속?  ──── 예 ──►  신경망 (심박 예측)
   문제  ──►  정답이 소수/온라인?  ─── 예 ──►  Bayesian (개인 경계)
             학습할 게 없음(결정론)? ─ 예 ──►  규칙 (판정)
             자연어 표현?  ──────── 예 ──►  LLM (코칭 문장)
```

---

# Part 3. 우리가 푸는 문제와 설계 여정

## 3-1. 문제 정의

**"손목 심박과 폰만으로, 랩 없이, 이 사람의 Zone 2를 공식보다 정확히 추정하고, 실시간으로 코칭한다."**

- 정답(참 LT1)이 없다(C04) → 지도학습을 직접 못 한다.
- 개인차가 크다 → 일괄 공식으로 안 된다.
- 심박은 늦게 반응한다 → 사후 코칭은 늦다.
- 손목 하나로 실행해야 한다 → 온디바이스, 가벼워야 한다.

## 3-2. 실패에서 배운 설계 (판정 신경망의 몰락)

이 프로젝트의 가장 값진 교훈이자, 심사에서 "왜 이렇게 했나"에 답이 되는 이야기다.

1. **처음엔 판정을 신경망으로 했다**: "지금 미달/유지/초과"를 다변량 신경망(MLP)이 3분류하게 했다.
   정답 라벨이 없으니 **물리 시뮬레이터로 라벨을 합성**해 학습시켰다.
2. **실기기에서 결함 발견**: "지속 심박이 상한을 42bpm이나 넘었는데 판정=미달"이 나왔다. 원인은
   **시뮬레이터 라벨 순환** — 시뮬 안에서 "심박 급강하 ↔ 곧 존 아래로 감"이 상관돼, 모델이 **추세로
   현재 위치를 무시하는 지름길**을 배웠다.
3. **검증까지 해봤다**: 심박 예측/판정 같은 태스크는 ablation에서 **선형 회귀와 신경망이 거의 동급**
   이었다(신경망 이득 수 %). 즉 그 태스크엔 신경망이 굳이 필요 없었다.
4. **역할 재분리**: 신경망을 **판정에서 빼서 "심박 예측"으로** 옮기고(연속 다수 데이터라 정당),
   판정은 **규칙**으로 확정(경계와 비교라 결정론, 모순 불가). 개인 경계 학습은 **Bayesian**이 맡았다.
5. 한때 신경망으로 "개인 역치"를 추정하려는 시도(adr-014)도 있었으나, 개인 데이터가 소수/온라인이라
   Bayesian이 맞는 도구여서 **강등**했다(기록은 보존).

> 이 여정이 곧 "AI≠신경망, 문제별 도구" 철학의 실증이다. **무작정 신경망을 쓴 게 아니라, 실패를
> 통해 각 도구의 자리를 찾았다.**

---

# Part 4. 시스템 전체 그림

## 4-1. 기기 배치 (워치 + 폰)

```
 ┌─────────────┐   심박/케이던스(/hr,/spm)   ┌────────────────────────┐
 │ Galaxy Watch│ ─────────────────────────► │  Galaxy Phone (두뇌/AI)  │
 │ (수집)      │ ◄───────────────────────── │  - 심박 예측 NN          │
 │ - 심박 센서 │   존 경계 동기화(/zones)     │  - 개인화 Bayesian       │
 │ - GPS/걸음  │   러닝 제어(/run/*)          │  - 규칙 판정             │
 │ - 토크테스트│                              │  - LLM 코칭             │
 └─────────────┘                              └────────────────────────┘
     화면off에도 측정 지속(포그라운드 서비스)      무거운 계산은 전부 폰
```

- **워치 = 수집 + 즉시 피드백 + 토크테스트 입력.** 화면이 꺼져도 심박을 계속 재서 폰으로 보낸다.
- **폰 = AI 두뇌.** 예측/개인화/판정/코칭을 다 한다. 무거운 모델은 폰에서 돌린다.
- 시뮬레이션 시엔 폰이 심박을 워치로 **미러링**해 워치에서도 토크테스트를 할 수 있다.

## 4-2. 1초에 무슨 일이 일어나나 (파이프라인)

매 1초(1Hz)마다 이 흐름이 돈다.

```
  심박 샘플 (워치 or 시뮬)
      │
  [이상치 가드] 40~220 밖이면 버리고 직전 유효값 사용
      │
      ├──────────────────────────────┬───────────────────────────┐
      ▼                              ▼                           ▼
  [규칙 판정]                    [심박 예측 NN]              [개인화 Bayesian]
  지속심박 vs 개인 경계          30/60초 뒤 심박 예측         토크테스트/드리프트로
  + 히스테리시스                 → "곧 초과" 선제 코칭        개인 경계 갱신(세션 내 실시간)
  → 미달/유지/초과               → "지금 Zone2 페이스" 제안   → 경계가 움직이면 밴드도 실시간 이동
      │
      ▼
  [코칭] 규칙이 방향 결정(초과→낮춰라) + LLM이 문장 표현 + 음성(TTS)
```

**개인 경계가 이 그림의 중심**이다. 판정도, 밴드 표시도, 예측의 목표 페이스도 전부 "지금 이 사람의
경계"를 기준으로 한다. 그리고 그 경계는 **Bayesian이 실주행으로 계속 학습**한다.

---

# Part 5. AI 컴포넌트 4개, 바닥부터

> **DP(Decision Point)가 무슨 뜻인가.** 각 DP는 "이 도구가 필요한 이유"를 정당화하는 게 아니라,
> **(1) 목적과 문제점을 정의하고 → (2) 그걸 푸는 후보 솔루션들을 놓고 → (3) 우리 QA 기준과 개발환경/
> 일정에 비추어 하나를 고른 결정**이다. 어떤 문제에 어떤 도구(NN/Bayesian/규칙/LLM)를 배정할지는
> 상위 결정(adr-016)에서 이미 정해져 각 DP의 **전제**로 들어온다. 즉 DP는 그 전제 위에서의 구체적
> 선택이다 — 예로 DP1은 "심박 예측에 NN을 쓴다"가 아니라(그건 전제), "그 NN을 **어떤 아키텍처**로
> 구현하나"의 결정이다.

## 5-1. 심박 예측 (DP1: 어떤 NN 아키텍처를 쓸지)

### 목적
최근 심박/페이스/경사/케이던스/경과시간을 보고 **30초 뒤, 60초 뒤 심박을 예측**한다(회귀). 심박이 늦게
반응(τ)하니, 넘고 나서가 아니라 **넘기 전에** 코칭하려면 예측이 필요하다.

### 전제 (DP1의 입력 — 이미 정해진 것)
- **왜 예측이 필요한가**: HR 동역학(지연 + 드리프트, 1-5) 때문에 "지금 심박"만으로는 코칭이 항상 늦다.
- **왜 NN인가**: 심박은 1Hz 연속 대량 데이터라 학습 재료가 충분하고, 지연/드리프트가 얽힌 비선형
  관계라 규칙 하나로는 표현이 어렵다(adr-016의 도구 배정). → **여기까지는 DP1이 정하는 게 아니라 전제다.**

### 문제점 (DP1이 실제로 결정하는 것)
그 예측 NN을 **어떤 아키텍처로 구현할 것인가**를, 우리 제약 아래에서 고른다:
- **온디바이스(폰) 실시간 추론** — 매 초 추론, 무거운 런타임 부담 회피.
- **학습셋 규모** — 실측 대규모 코퍼스가 아니라 물리 시뮬레이터 데이터 → 큰 모델은 과적합 위험.
- **QA/검증 가능성** — 라벨 순환(판정 NN의 결함, 3-2) 재발 방지, ablation으로 이득을 검증할 수 있어야.
- **개발 일정** — 학습/배포/온디바이스 추론까지 한 파이프라인으로 감당 가능해야.

### 후보 비교
| 후보 | 장점 | 우리 제약과의 적합성 |
|---|---|---|
| LSTM/RNN(GRU) | 시퀀스 예측에 강함, 문헌 선례(athlete_hr_predict LSTM, Zhu 2025 RNN) | 온디바이스 추론/학습 비용 큼, 소규모 시뮬셋엔 과할 수 있음 |
| **경량 MLP** | 순수 Kotlin 순전파로 온디바이스 초경량, 학습/배포 단순 | near-linear 태스크에 충분(아래 성적), 단조성 페널티로 역질의 가능 |
| 선형 회귀 | 가장 단순, 해석 쉬움 | 베이스라인. 비선형(지연+드리프트) 표현/역산엔 한계 |

### 결정: 경량 MLP
신경망은 "입력 숫자들을 여러 층에서 곱하고 더한 뒤, 음수는 0으로 꺾는(ReLU) 비선형 변환"을 반복해
출력을 낸다. 층 안의 곱하는 숫자(가중치)가 학습으로 조정되는 값이다.

```
 입력 7개 → [32칸] →ReLU→ [16칸] →ReLU→ [2칸]  →  (30초 뒤 심박, 60초 뒤 심박)
 (심박now/지속, 심박추세, 유지할 페이스, 경사, 케이던스, 경과시간)
```
- **왜 LSTM/RNN이 아니라 MLP인가**: 이 태스크는 ablation에서 **near-linear**로 확인됐다(선형과 큰 차이
  없음). 대형 시퀀스 모델의 이득은 작은데 온디바이스 추론/개발 비용은 크다 → 우리 QA/일정 제약에선
  경량 MLP가 최적점. 문헌의 LSTM/RNN은 "이 방향이 유효하다"는 선례로 인용하되 규모는 우리 제약에 맞춰 축소.
- **왜 선형이 아니라 MLP인가**: 지연/드리프트의 비선형을 담고, **단조성 페널티**(느린 페이스 → 낮은 예측
  심박 강제)로 예측을 거꾸로 돌려 **"Zone 2 유지 페이스"를 역산**하려면 비선형 함수가 필요하다.
- 출력은 확률(분류)이 아니라 **두 개의 수(회귀)**다(softmax 없음). **라벨 = 실제 미래 심박**이라
  판정 MLP에서 겪은 **라벨 순환 결함이 원천적으로 없다**. 학습은 `ml/train_hr_dynamics.py`(PyTorch),
  결과 JSON을 앱이 로드해 **순수 Kotlin 순전파**로 추론(무거운 TFLite 런타임 없이 곱셈/덧셈만).

### 성적 (정직하게) — 이게 곧 DP1 결정의 근거다
아래는 **개인 보정을 얹기 전, 기본 모델(평균 러너)** 성적이다(개인 보정 효과는 5-1 마지막에서 따로).
| 예측 지평 | 기본 MLP RMSE | "지금값 유지" 기준선 | 개선 |
|---|---|---|---|
| 30초 뒤 | **14.0 bpm** | 17.0 bpm | 약 18% |
| 60초 뒤 | **20.4 bpm** | 30.2 bpm | 약 32% |
- 페이스 단조성 위반율 0.0% (역질의가 성립).
- **정직한 단서**: 잘 만든 선형 회귀와 큰 차이는 안 난다 → 이 **near-linear 성질이 곧 "대형 시퀀스 NN
  대신 경량 MLP"라는 DP1 결정의 핵심 근거**다. (near-linear라는 것도 ablation으로 정직하게 확인한 사실이다.)

### 용도
- **선제 코칭**: 판정이 아직 "유지"라도 60초 뒤 예측이 상한을 넘으면 "이대로면 곧 초과, 미리 늦추세요".
- **목표 페이스 제안**: "지금 컨디션에 Zone 2 유지하려면 6'30"/km" 처럼 실행 가능한 숫자로.

### 온라인 개인 보정 — 예측이 틀리면 배운다 (spec-018, adr-019) ★새로 도입한 ML
기본 NN은 시뮬로 학습된 **"평균 러너"** 모델이라, 개인의 심박 동역학(반응 속도 τ/드리프트/피로)과
어긋날 수 있다(위 20.4bpm은 이 평균 모델 성적이다). "페이스는 그대로인데 60초 뒤 예측과 실제가 크게
다르다"는 건 **이 사람이 평균과 다르다**는 신호다 — 이걸 버리지 않고 배운다.

**쉽게(내비 비유)**: 내비가 "10분"이라는데 당신은 늘 8분에 도착한다면, 앱이 그 패턴을 배워 다음부터
"8분"으로 고쳐준다. 남들 평균이 아니라 **당신 패턴**으로. 심박 예측도 똑같이 개인 오차를 배워 뺀다.

- 매초 예측을 버퍼에 저장 → 30/60초 뒤 실제 심박이 오면 **오차 = 실제 − 예측**을 계산 → 개인 잔차
  모델을 **온라인(LMS)으로 갱신** → 다음 예측 = 기본 NN + 개인 보정. (LMS = 최소평균제곱, 경사하강의
  가장 단순한 형태 — 오차 방향으로 가중치를 조금씩 민다.)
- **online = 인터넷 아님**: 데이터가 실시간으로 하나씩 올 때마다 즉석 학습한다는 뜻. 전부 폰 안에서 돈다.
- **정직한 조건**: 예측은 "이 페이스 유지 시" 조건부라, **페이스가 유지된 샘플만** 학습에 쓴다(페이스를
  바꿨으면 예측이 틀린 게 아니라 조건이 바뀐 것). 세션 종료 시 보정 가중치를 저장해 **다음 세션이 이어서 학습**.
- 이건 DP2(Bayesian 경계)와 **같은 철학** — "평균 prior + 온라인 개인 보정". 즉 예측에도 개인화를 얹었다.

**검증(폐루프, EXPERIMENT_LOG §12)**: 기본 모델과 심박 동역학이 다른 가상러너 7명을 폐루프로 5세션씩
돌려(가중치 이월) 마지막 세션 RMSE를 비교했다. **60초 예측 RMSE 평균 27→12.5bpm(-53%), 7/7 러너 개선.**
기본 모델은 개인이 평균과 다를수록 22~36bpm으로 나빠지는데, 개인 보정이 8~13bpm으로 되돌린다.
단 **정직하게**: 이건 시뮬 숫자라 낙관적이다(실사람은 더 지저분). 보정은 세션 몇 회 누적돼야 효과가
크고, 세션 초반엔 보정≈0이라 기본 모델과 같다. 실주행 로그로 채점하는 게 다음 수순이다.

## 5-2. 개인 Zone 2 학습 (DP2: 도구 선택 — 온라인 Bayesian)

### 목적
"이 사람의 Zone 2 상한이 몇 bpm인가"를 **실주행 데이터로 세션마다 좁혀** 개인 경계로 수렴시킨다.
**이게 이 앱의 진짜 "개인화 학습"이다.**

### 문제점과 후보 — 왜 신경망이 아니라 Bayesian인가 (핵심 판단)
- **개인 데이터가 적다**: 한 사람 세션의 라벨은 몇 개뿐 → 신경망은 수천 개가 필요 → 과적합(외움).
- **정답이 온라인으로 하나씩** 들어온다(러닝 중 말하기 테스트) → 재학습이 아니라 온라인 갱신이 맞다.
- **콜드스타트**: 신규 유저는 데이터 0 → 공식값에서 시작해야 한다.
- **불확실성 관리**가 필요하다(얼마나 확신하는지).
- 이 넷을 만족하는 도구가 신경망이 아니라 **온라인 Bayesian(켤레 가우시안)**이다.

> 참고: 선행연구 중엔 젖산역치를 **신경망**으로 추정한 것도 있다(Etxegarai 2018, Zhu 2025). 우리는
> 그걸 검토했지만 위 이유로 **신경망 대신 Bayesian**을 택했다. 즉 그 논문들은 우리 방법의 근거가
> 아니라 "다른 선택을 한 대조군"이다.

### 정답(라벨)은 어디서 오나 — 토크 테스트 (5단계, spec-016)
랩 젖산이 없으니, **"편하게 말할 수 있는 마지막 강도" ≈ VT1/LT1**이라는 생리학 근거(토크 테스트)를
라벨로 쓴다. 러너가 러닝 중 대화 체감을 **5단계**로 누르면, 각 단계를 서로 다른 관측(경계 추정치 z와
그 신뢰도 σ)으로 바꿔 Bayesian에 넣는다. 강도 오름차순으로 z가 단조 감소한다.

| 단계 | 의미 | 관측 z(bpm) | σ(bpm) | 뜻 |
|------|------|-------------|--------|-----|
| 아주 편함 | 노래도 가능 | 현재+10 | 16 | 경계가 현재보다 한참 위(먼 단측 증거 → 넓은 σ) |
| 편함 | 편하게 대화 | 현재+5 | 14 | 경계가 위(약한 단측 증거) |
| 보통 | 말이 끊기기 시작 ≈ VT1 | 현재 | 6 | 지금 지속 심박을 경계로 **직접 관측**(가장 좁은 σ, 최고 확신) |
| 벅참 | 짧게만 말함 | 현재−5 | 14 | 경계가 아래(약한 단측 증거) |
| 매우 벅참 | 거의 못 말함 | 현재−10 | 16 | 경계가 한참 아래(먼 단측 증거 → 넓은 σ) |

**왜 5단계인가**: 처음엔 3단계(편함/보통/벅참)였는데, "아주 편함"과 "겨우 편함"이 같은 관측으로
뭉개져 해상도가 낮았다. 5단계로 늘려 라벨 해상도를 높였다. 중앙(보통)만 직접 관측이라 σ가 가장
좁고(확신 최고), 극단은 "경계가 멀다"는 방향만 알 뿐 거리를 몰라 σ를 넓혀 과신을 막는다. 폰은 5칩,
워치는 화면이 작아 강한 중간 3칩(편함/보통/벅참)만 둔다.

**중요**: 이 판단은 "그 사람의 진짜 임계 대비 체감"이다. **심박 숫자가 낮아도 그 사람껜 벅찰 수 있다**
(개인마다 임계가 다르니까). 그래서 심박 높낮이와 무관하게 수집한다.

**단측(one-sided) 관측 — 부등식으로만 반영 (2026-07-06 정정)**: 토크 테스트는 사실 "임계가 지금 심박보다
위냐 아래냐"의 **부등식** 정보다. 편함=임계≥현재(하한), 벅참=임계≤현재(상한), 보통=임계≈현재(점).
그래서 **편함류는 경계를 올리는 방향만, 벅참류는 내리는 방향만** 반영한다(반대 방향은 새 정보 없음 → 무변화).
안 그러면 "편한데 미달"(현재 심박 < 경계)에서 편함이 경계를 잘못 끌어내린다 — 편함은 "임계가 현재보다 위"라는
하한일 뿐, 임계가 낮다는 뜻이 아니기 때문. **올바른 사용법**: 판정이 실제 느낌과 다르면 솔직히 답한다 —
벅찬데 아직 Zone 2로 나오면 '벅참'이 상한을 낮추고, 편한데 초과로 나오면 '편함'이 상한을 올린다.

보조로 **디커플링(드리프트)** 관측도 쓰지만, 편향(Conconi)이 있어 **아주 약하게만**(σ=20) 반영한다.
주역은 토크 테스트다.

### 왜 "주관 설문"인가 — 객관화를 시도했으나 기각 (adr-018)
토크 테스트가 자기보고(주관)라 재현성이 약하다는 건 안다. 그래서 이를 **객관 신호**로 바꾸려고 별도
PoC(`voice-poc`)에서 세 가지를 실기기 검증했고, **모두 온디바이스에서 신뢰 불가**로 확인해 기각했다.

| 시도 | 방법 | 기각 사유(실측) |
|------|------|-----------------|
| 음향 타이밍 | 완독시간/끊김/발화량 VAD | "헉헉"이 발화로 잡혀 숨참 미포착, 고정 녹음창에서 완독시간 무의미 |
| ASR 완성도 | 온디바이스 음성인식 → 문장 완성도 | 짧은 문장은 힘들어도 완독되어 "편함" 오판 |
| 호흡 감지(ML) | YAMNet(AudioSet 521클래스) 온디바이스 | 폰 마이크 날숨을 **Beatboxing/Music/Speech로 오분류**, 호흡 클래스 안 켜짐 |

문헌([Respiratory Rate from Breathing Sounds, PMC10296589])도 러닝 소음이 최대 오차원이라 **마스크
내장 마이크**가 필요하다고 보고한다. 즉 폰/워치 마이크로 호흡을 온디바이스 감지하는 것은 우리 구현의
문제가 아니라 **현 단계 기술의 한계**다. → 무비용이면서 견고한 **주관 5단계 설문이 최적해**로 남는다.
이 탐색 자체가 adr-016 "문제에 맞는 도구" 원칙의 실증이다: 객관화를 위해 ML/ASR/DSP를 다 시도하고
데이터로 부적합을 확인한 뒤 주관 신호로 회귀했다.

### 어떻게 (Bayesian 갱신, 숫자로)
개인 경계를 **분포**로 들고 있다: `경계 ~ 정규분포(평균 μ, 불확실성 σ)`.
```
 관측 z(bpm)와 그 신뢰도 σ_obs가 들어오면:
   새 μ = (기존 μ와 z를, 각자의 확신도 1/σ² 로 가중평균)
   새 σ = (관측을 반영해 더 좁아짐 = 확신 증가)
```
= 저울질이다. 확신이 큰(σ 작은) 쪽을 더 믿는다. 세션이 쌓일수록 σ가 줄고(확신↑) 참 경계로 수렴한다.

### 안전장치와 누적
- **세션 내 이동 제한 ±10bpm**: 한 번의 관측에 휘둘리지 않게(점진적 학습).
- **절대 범위 clamp**: 경계 비율을 0.30~0.75 HRR로 제한(생리적으로 말이 되는 범위).
- **세션 종료 시 최종 경계 저장**(`LearnedZone`) → 다음 세션이 거기서 시작 → **세션을 넘어 누적**.
  "편한데 미달로 나오면 '편함'을 누르면 다음 세션부터 내려간다."

### 실시간성
경계가 움직이면 화면의 Zone 2 밴드/상단 숫자가 **러닝 중 즉시** 따라 이동한다(매 초 현재 경계로 렌더).
말하기 테스트를 누르면 그 순간 밴드가 눈에 띄게 바뀐다.

## 5-3. Zone 2 판정 (DP3: 도구 선택 — 규칙)

```
 지속 심박(최근 60초 평균)  vs  개인 경계[하한, 상한]
   낮으면 → 미달 / 안이면 → 유지 / 높으면 → 초과
   + 히스테리시스: 경계 ±2bpm을 확실히 넘을 때만 상태 전환(경계에서 깜빡임 방지)
```
- **왜 규칙?** 판정과 화면 밴드가 **같은 경계**를 보므로 "밴드는 초록인데 판정은 초과" 같은 모순이
  **구조적으로 불가능**하다. 학습할 게 없고(경계 밖이냐), 결정론이라 설명 가능하고 안전하다.
- **AI를 안 쓰는 것도 설계 판단.** (판정에 신경망을 뒀다가 라벨 순환 결함을 발견하고 규칙으로 바꾼
  것이 Part 3-2의 이야기.)
- 판정의 어려운 부분(심박 지연 등 비선형)은 별도로 **심박 예측 NN(DP1)**이 다뤄 선제 코칭에 쓴다.

## 5-4. 코칭 문장 (DP4: 도구 선택 — 규칙 방향 + LLM 표현)

```
 판정/예측 → [규칙이 방향 결정]  초과→"낮춰라" / 미달→"올려라" / 곧초과→"미리 낮춰라"
           → [LLM(Gemini Nano)이 자연스러운 한 문장으로 표현]
           → [출력 가드] 방향 모순/이모지 제거, 실패 시 정적 규칙 문장 폴백
           → 음성(TTS)
```
- **방향은 규칙이 정하고, LLM은 표현만** 한다. 그래서 LLM이 엉뚱한 방향을 말하는 걸 가드가 막는다.
- 온디바이스(폰 안 Gemini Nano)라 오프라인에서도 동작, 미가용 시 규칙 문장으로 폴백.
- 정지 시엔 "이제 출발해 볼까요", 오래 안 물었으면 토크 테스트 유도 등 상황 코칭도 있다.
- **프롬프트는 외부화**(`assets/coach_prompt.json`)돼 코드 없이 문구 편집 가능하고, 프롬프트에
  현재 심박/개인 목표/60초 예측 같은 **맥락 수치**를 넣어 표현이 구체적이다("110-123bpm 사이로 유지해요").
- **기온 맥락(더위 28℃+)**: 더우면 심박·드리프트가 커지므로 "무리 말고 수분" 같은 조언을 덧붙인다.
  단 이것도 **방향이 아니라 표현 재료** — 가드가 폼/기온 절을 방향 판정에서 제외해 안전을 지킨다.

## 5-5. 콜드스타트: 프로필로 첫 추정

데이터가 0인 신규 유저도 첫날부터 동작해야 한다. 프로필(나이/안정심박/최대심박 + 체형/러닝수준/빈도)로
**첫 경계(prior)**를 계산한다.
```
 Zone2 상단 = 최대심박 × 0.70 (유산소 임계 LT1 근사, 체형/수준/빈도로 ±미세조정)
 이를 내부 HRR 비율로 환산: uFrac0 = (상단bpm − 안정심박) / 여유심박,  0.30~0.75로 제한
 밴드 폭 = 0.12 × 여유심박 (≈ 최대심박 10%)
```
예) 최대 200, 안정 60인 사람: 상단 = 0.70×200 = 140, 하단 ≈ 123 → **Zone 2 = 123~140 bpm**.
이 값에서 시작해, 실주행 토크 테스트로 Bayesian이 그 사람 몸에 맞게 좁혀간다.

## 5-6. 개인화 관리와 설명 — 프로필 + 시각화 + AI 설명 (spec-020, QA6 설명용이성)

개인화가 "쓸수록 학습"되는데 그게 안 보이면 못 믿는다. 그래서:
- **다중 프로필**: 이름 붙인 프로필 여러 개(생성/전환/삭제/초기화, 홈에서 러닝 전 선택). 각 프로필이
  독립된 신체정보 + 학습 데이터(경계 이력/예측 보정)를 가진다. 기존 사용자는 "기본"으로 무손실 승계.
- **진행 시각화**: 초기(공식) Zone 2 밴드와 현재(학습) 밴드를 겹쳐 그려 **상단이 몇 bpm 이동했는지** +
  세션별 경계 스파크라인 + 근거(러닝 N회·말하기 M회·신뢰도 σ).
- **AI 설명(QA6)**: "왜 이렇게 보정됐나"를 Gemini Nano가 설명하되, **사실은 규칙이 확정하고 LLM은 표현만**
  (코칭 방향 잠금과 같은 무결성 — 지어낸 수치 0). 미가용 시 규칙 문장 폴백.

---

# Part 6. 검증 — 가상러너와 폐루프

정답이 없는 문제를 어떻게 검증하나? 우리는 **가상러너**를 프로그램적으로 만들어, 실기기 없이도
**전체 폐루프**를 돌려본다. 이건 QA5(테스트 가능성)에 직접 부합하는 설계다.

- **엔진과 러너를 분리**했다(결합도↓). 시뮬 엔진(물리)이 `VirtualRunner`(신체/스타일/코칭 반응성/진짜
  임계)를 받아 심박과 페이스를 생성한다.
- **폐루프**: 앱이 "초과" 판정 → 가상러너가 반응성만큼 페이스를 낮춤 → 심박 변화 → 가상러너가
  **내부적으로 토크 테스트에 응답**(그 러너의 진짜 임계 대비 현재 심박) → 개인화가 학습.
- 가상러너의 **진짜 임계(정답)**를 우리가 알고 있으니, **앱의 개인화가 그 임계로 수렴하는지**가 곧
  개인화 기능의 검증이 된다.

**사람처럼 강화(spec-019)**: 실주행을 못 하니 이 계측기를 진짜 사람에 가깝게 키웠다. 러너 특성을
**설정 가능한 범위**로 정의하고, 세션마다 그 범위 안에서 randomness가 발현된다:
- 신체/스타일: 심박 반응 속도, 드리프트, **피로 누적**, 페이스 규율, 케이던스, **코칭 반응 지연**.
- 환경: **지형(언덕 프로파일 → 경사를 실제로 만들어 오르막 판정/코칭을 검증)**, **기온(더울수록 드리프트↑)**, 그날 컨디션.
- 돌발: 신호등/추월 같은 **서지·감속**, **센서 아티팩트**(접촉 불량 드롭아웃/스파이크 → 이상치 가드 검증).
- 토크 테스트에 **개인 편향**(과대/과소 보고)까지.
- 프리셋 7종(균형형/초보 과속형/베테랑 절제형/코칭 잘 따름/**더위 취약형/언덕 코스/불안정 센서**) +
  **🎲 랜덤 러너**(체력을 잠재변수로 상관된 그럴듯한 사람을 매번 새로 샘플). 모두 seed 기반이라 재현 가능.

```
   [앱 코칭] ──► [가상러너 페이스 조절] ──► [심박 변화] ──► [가상러너 토크테스트]
      ▲                                                          │
      └────────────── [개인화 Bayesian 학습] ◄────────────────────┘
              (앱의 경계가 러너의 '진짜 임계'로 수렴하면 성공)
```

이 강화된 러너로 **예측 온라인 보정도 폐루프로 정량 검증**했다(§12: 60초 RMSE 27→12.5bpm, 7/7 개선).
이 밖에 심박 예측 NN은 baseline 대비 RMSE로, 규칙 판정은 "모순 불가" 속성 테스트로, 파이프라인은
통합 테스트로 검증한다(본 앱 단위 테스트 **63건**: 토크 5단계+단측 관측, 예측 보정 하네스, 가상러너 범위/지형/
센서 아티팩트, 경사 노이즈 강건성 등 포함).

---

# Part 7. 정직한 한계

이 프로젝트는 한계를 숨기지 않는 걸 원칙으로 한다.
- **절대 정확도는 검증 불가**: 참값(LT1)이 랩에만 있으니(C04), "우리 경계가 진짜와 몇 bpm 차이인가"는
  소비자 환경에서 잴 수 없다. 그래서 성능은 **방향 정확성 + 수렴 메커니즘 + 무랩 서로게이트 정합**으로만 평가한다.
- **심박 예측 NN은 선형과 큰 차이 안 남**: 신경망 선택의 근거는 "유일 해법"이 아니라 연속 데이터 +
  문헌 + 활용이다(정직하게 ablation으로 확인).
- **토크 테스트가 주관 라벨**: 자기보고라 재현성이 약하다. 객관화(음향/ASR/호흡 ML)를 실기기로
  시도했으나 폰 마이크로는 온디바이스 신뢰 불가여서 기각했다(adr-018, Part 5-2). 주관이지만 무비용
  견고한 최적해로 남았다.
- **디커플링 관측 편향**: Conconi류 편향이 있어 약한 보조로만 쓴다. 주 라벨은 토크 테스트.
- **시뮬 학습의 한계**: 시뮬 물리가 실제 생리보다 단순하다. 예측 보정의 -53%도 시뮬 낙관치 —
  실주행 로그로 채점하는 게 다음 수순이다.
- **GPS 경사는 근본적으로 부정확**: 소비자 GPS 고도 오차(±10~30m)를 순간 미분하면 경사가 수백 %로
  튄다(3% 언덕은 노이즈에 묻힘). 정량상 ±12m 노이즈를 평지 ±2%로 누르려면 창이 ~200m 필요 —
  그래서 **거리창 회귀(150m)로 거시 경향만** 보고, 정밀 %가 아니라 범주(오르막/평지/내리막)로만 쓴다.
  정공법은 기압계(고도계)이며 후속 과제로 둔다.
- **DFA-α1 미채택**: 참값에 더 가까운 HRV 지표지만 손목 RR 품질/계산 부담으로 현 단계 미사용(향후 채널).

> **핵심 태도**: "정답 없는 개인 문제"에 대해 **공식보다 낫게, 그 사람의 실제 반응에 맞춰 좁혀가는**
> 정공법을 택했고, 못 하는 것은 못 한다고 명시한다.

---

# 부록 A. 용어사전

- **Zone 2**: 유산소 기초 강도. LT1 바로 아래. "대화 가능한" 강도.
- **LT1(1차 젖산역치)**: 젖산이 처음 의미 있게 오르는 지점. Zone 2 상단의 생리적 정의. 참값은 랩만.
- **HRmax / RHR / HRR**: 최대심박 / 안정시심박 / 여유심박(HRmax−RHR).
- **%HRmax / %HRR(Karvonen)**: 강도 표현 두 방식. 우리는 %HRmax 기준(상단 70%).
- **토크 테스트**: 대화 가능성으로 강도를 자가관측. ≈ VT1/LT1. 우리 개인화의 주 라벨.
- **카디악 드리프트**: 같은 페이스인데 심박이 서서히 오르는 현상(혈역학적).
- **신경망/회귀/분류**: 비선형 함수 근사 / 수를 예측 / 범주를 맞힘. 우리 NN은 회귀(심박 예측).
- **Bayesian/켤레 가우시안**: 관측으로 추측+불확실성을 갱신. 우리 개인화 도구.
- **온라인 학습(online learning)**: 데이터가 실시간으로 하나씩 올 때마다 즉석에서 조금씩 배우는 방식.
  **인터넷과 무관**(오프라인/비행기 모드에서도 동작). 반대는 오프라인/배치(다 모아 한 번에 학습).
- **LMS(최소평균제곱)**: 온라인 학습의 가장 단순한 형태. 오차 방향으로 가중치를 조금씩 미는 경사하강.
  우리 심박 예측 개인 보정에 사용.
- **히스테리시스**: 경계에서 상태가 깜빡이지 않게 여유를 두는 것(±2bpm).
- **RMSE/persistence**: 예측 오차 지표 / "지금값 유지"라는 순진한 기준선.
- **콜드스타트**: 데이터 0인 신규 유저 상황. 공식 prior로 대응.
- **C04**: 참값 부재(랩 없이 진짜 Zone 2를 모름)라는 우리 제약.

# 부록 B. 파일 지도 (코드를 볼 때)

- 심박 예측 NN: `ml/train_hr_dynamics.py`(학습), `app/.../pipeline/HrDynamics.kt`(폰 추론)
- 예측 온라인 개인 보정: `app/.../pipeline/HrPredictionLearner.kt`, 누적 저장 `app/.../data/LearnedDynamics.kt` (spec-018/adr-019)
- 개인화 Bayesian: `app/.../pipeline/Personalization.kt`, 누적 저장 `app/.../data/LearnedZone.kt`
- 규칙 판정: `app/.../pipeline/ZoneJudge.kt`
- 코칭: `app/.../coaching/`(RuleCoach/LlmCoach/DirectionGuard)
- 콜드스타트 prior: `app/.../domain/Zone2Prior.kt`
- 토크 테스트 5단계: `app/.../pipeline/Personalization.kt`(TalkState/observeTalkTest), `app/.../RunActivity.kt`(칩)
- 가상러너/폐루프: `app/.../domain/VirtualRunner.kt`(강화·랜덤샘플러), `app/.../sim/SimRunnerSource.kt`(spec-019)
- 예측 보정 검증 하네스: `app/.../test/.../HrPredictionCorrectionHarnessTest.kt` (EXPERIMENT_LOG §12)
- 경사(거리창 회귀): `app/.../sensor/SlopeEstimator.kt`
- 코칭 프롬프트 외부화: `app/.../assets/coach_prompt.json`(문구 편집), 로더 `app/.../coaching/CoachPrompt.kt`
- 밴드 게이지(평균/실측/예측 3마커): `app/.../ui/ZoneBandView.kt`
- 시뮬레이터/실험: `ml/simulator.py`, `ml/EXPERIMENT_LOG.md`
- 객관화 탐색(기각): `voice-poc/`(음향 VAD/ASR/YAMNet), 결론 `arch/adr-018`
- 설계 문서: `arch/adr-016`(도구 선택), `arch/adr-013`(판정=규칙), `arch/adr-004`(Bayesian),
  `spec/spec-016`(토크 5단계), `arch/adr-018`(객관화 기각),
  `arch/zone2-physiology-and-estimation.md`(생리 근거), `arch/architecture-overview.md`(구조)

# 부록 C. 참고문헌

- San Millán & Brooks (2018) — Zone 2 대사와 미토콘드리아 기능, LT1.
- Seiler et al. (2014), Int. J. Sports Physiol. Perform. — 양극화 훈련(엘리트 ~80% 저강도).
- HR on-kinetics (mono-exponential τ) 및 Cardiac Drift(혈역학적) 생리학 문헌.
- Rogers et al. (2021), Front. Sports Act. Living — DFA-α1 0.75 ≈ 유산소 임계(VT1).
- 토크 테스트 ≈ VT1 검증 연구(체계적 리뷰 포함).
- (심박 예측 NN 근거) athlete_hr_predict(LSTM, MIT), Zhu et al. (2025) — 웨어러블 러닝 심박/역치 예측(RNN).
- (대조군) Etxegarai et al. (2018) — 젖산역치를 RNN으로 추정. 우리는 소수/온라인 라벨이라 Bayesian을 택함.

> 폐기/강등 이력: 판정 MLP(`Zone2Classifier`)는 라벨 순환 결함으로 Superseded(→규칙 판정 ZoneJudge),
> 역치 추정 NN(`ThresholdEstimator`)은 Bayesian(Personalization)과 역할 중복이라 Demoted.
> **둘 다 앱 코드/에셋에서 제거(2026-07-06)** — 결정 근거와 실험은 `arch/adr-013`/`adr-016`/
> `ml/EXPERIMENT_LOG.md` §8·§10과 git 이력에 보존. 학습 스크립트(`ml/train_mlp.py` 등)만 실험 기록으로 남긴다.
