# 개인화 접근 A vs B — 실증 비교

두 개인화 접근을 같은 시뮬레이터/같은 test 러너로 비교한다 (adr-006).

- **A. 직접 Zone 분류** (MLP, `train_mlp.py`) — 다변량 특징 → 3분류. 개인 경계로 입력 정규화.
- **B. HR 회귀 + 잔차 개인화** (`hr_regressor.py`, 성시원 제안) — 맥락 → 예상 HR 회귀, 잔차로 개인화, 경계 도출 후 판정.

작성: 2026-07-02. 시뮬레이터 60명/360세션, 러너 단위 동일 분할.

---

## 결과

| 축 | A (분류) | B (HR 회귀) |
|------|:---:|:---:|
| 코칭 방향 정확성 (QA1) | **0.996** | 0.695 (decoupling 경계) / 0.899 (좋은 경계 가정) |
| Zone2 상한 추정 오차 (bpm) | (경계 내재) | 공식 4.1 / 오프셋 43.6 / decoupling 21.9 |
| HR 예측 오차 MAE (bpm) | - | 48.7 → **23.2** (개인화로 감소) |
| 실데이터 학습 가능 | 어려움 (참 Zone 라벨 없음 → 시뮬 의존) | **가능** (정답=실측 HR) |
| 해석가능성 | 낮음 (블랙박스) | **높음** (예상 HR/편차/경계) |
| 부가 산출 | 없음 | **피트니스(VO2max 상관), HR 예측** |

---

## 발견

1. **B의 개인화 메커니즘은 유효** — 잔차 보정이 HR 예측오차를 48.7→23.2 bpm로 낮춤. 정답(실측 HR)이 측정 가능해 실데이터로 학습·검증 가능(직접분류 A의 최대 약점 해소). (문헌 개인화 HR모델 중앙오차 ~6bpm; 본 시뮬레이터는 base_pace가 잠재변수라 오차가 큼)
2. **제안 원안(잔차 오프셋으로 Zone2 이동)은 실패** (43.6 bpm) — HR-페이스 오프셋은 "속도 체력"을 반영할 뿐 "유산소 임계 위치"가 아니다. 임계 도출엔 decoupling/편향점 신호가 별도로 필요(리서치와 일치).
3. **임계 추정은 어렵다** — decoupling 기반 추정(21.9bpm)이 현재 조악하여 공식(4.1bpm)보다도 나쁨. 임계 추정기 성숙이 B의 핵심 과제.
4. **A의 다변량 판정이 방향 오판을 더 잘 막는다** — 좋은 경계를 동일하게 줘도 A 0.996 vs B(단일 임계) 0.899. 다신호(dHR/decoupling/slope) 결합이 경계 근처 오판을 줄임.
5. **testbed 한계(중요)** — 본 시뮬레이터는 참 임계가 공식(0.70)에서 거의 안 벗어나(σ≈6bpm) 공식이 이미 4bpm으로 정확하다. 즉 **임계 개인화가 이길 여지가 작은 환경**이라, B(및 개인화 일반)에 불리하게 세팅돼 있다. 공정한 비교엔 개인 편차가 큰 시뮬레이터 또는 실데이터가 필요.

---

## 개인 임계 변동 스윕 (compare_ab.py) — 공정 비교

개인 임계 변동(u_frac_sigma)을 키우면 공식이 점점 부정확해져 개인화 헤드룸이 커진다. 이 조건에서 A/B 재비교:

| σ(임계변동) | A 코칭방향 | B 코칭방향 | B HR MAE(pop→pers) | 임계오차 공식/decoup |
|:---:|:---:|:---:|:---:|:---:|
| 0.045 (작음) | 0.996 | 0.695 | 48.7→23.2 | 4.1 / 21.9 |
| 0.090 (보통) | 0.997 | 0.579 | 86.4→15.9 | 8.2 / 26.2 |
| 0.140 (큼)   | 0.999 | 0.457 | 95.2→11.2 | 12.7 / 30.9 |

- **A는 모든 변동에서 견고** (0.996→0.999). 개인 경계 정규화가 변동을 흡수.
- **B의 개인화 핵심은 검증됨**: 변동이 클수록 개인화가 HR 예측을 더 크게 개선(pers MAE 23→11). "개인차가 클수록 개인화가 더 값지다"는 제안 취지 그대로.
- **B의 임계 추정(decoupling)은 미성숙**: 헤드룸이 커져도 개선되지 않고 오히려 악화(방향 0.70→0.46). B를 완결적 판정기로 쓰기 어렵게 하는 실제 병목.
- 주의: B의 HR MAE 절대값(48~95)은 시뮬레이터가 base_pace를 잠재변수로 둔 탓에 부풀려짐(모집단 모델이 pace→HR를 못 맞춤). **개인화의 상대적 개선 추세**가 유효 신호.

---

## 결론

- **판정(최종 결정)**: **A(다변량 분류)가 명확히 우수하고 변동에 견고** (코칭 방향 0.996~0.999). 이 프로젝트 목표(판정→코칭)엔 A가 주 판정기로 적합.
- **개인화(적응)**: **B의 잔차 개인화 메커니즘은 유효**하며 개인차가 클수록 값지다. 실측 HR로 학습 가능(A의 라벨 문제 해소) + 피트니스 부산물.
- **B의 병목**: 데이터로 Zone2 임계를 뽑는 부분(decoupling 추정)이 미성숙. 이게 풀려야 B가 완결적 판정기가 됨.

→ **결정 방향**: **주 판정기는 A.** B의 개인화 HR 모델은 (i) 개인 경계/적응 강화(DP3)와 (ii) 피트니스 추적의 **보조 모듈**로 채택. 하이브리드지만 무게중심은 A. (adr-006 결정에 반영)

## 다음 단계
- 본 프로젝트: A를 판정기로 확정, 개인화 적응(Bayesian) 검증 진행.
- (선택) decoupling/편향점 임계 추정기 개선 — B를 완결적 판정기로 발전시키려면.
- 시뮬레이터 base_pace를 입력/추정 가능하게 → B HR모델 절대성능 현실화.
- 실제 필드 데이터 확보 시 B의 실데이터 학습 강점 실증.
