# Zone2Runner 공부 가이드 — 이 프로젝트, 바닥부터 이해하기

> **누구를 위한 문서?** 이 프로젝트의 기술적인 부분(특히 AI)을 잘 모르는 상태에서, 하나씩 공부하며
> "내가 만든 것을 내가 설명할 수 있는" 수준까지 가기 위한 개인 학습서.
> 사전지식 없다고 가정하고 씀. 수식은 최소, 비유는 최대.
>
> **같이 볼 문서**: `ml/AI_EXPLAINED.md`(AI 개념 요약판). 이 가이드는 그보다 넓고(앱 전체) 깊게(코드까지) 간다.

---

## 0. 공부 순서 (추천 커리큘럼)

하루에 한 챕터씩 봐도 되고, 한 번에 쭉 읽어도 된다. **순서는 지키는 걸 추천** — 뒤로 갈수록 앞 개념을 쓴다.

| 단계 | 챕터 | 내용 | 예상 시간 |
|:---:|------|------|:---:|
| 1 | §1~2 | 큰 그림 + 러닝/심박 기초 용어 | 30분 |
| 2 | §3 | 데이터의 여행(1초 동안 일어나는 일) | 30분 |
| 3 | §4 | **AI ① 심박 예측 신경망(딥러닝) + 규칙 판정** — 제일 중요 | 2시간 |
| 4 | §5 | AI ② 베이지안 개인화 (이 앱의 진짜 개인화 학습) | 1시간 |
| 5 | §6 | AI ③ LLM 코칭 | 30분 |
| 6 | §7 | 시뮬레이터(학습 데이터 공장) | 1시간 |
| 7 | §8 | 앱 구조(화면/저장/센서 교체) | 1시간 |
| 8 | §9 | 직접 해보기(실습) | 2시간+ |
| 9 | §10~12 | 용어 사전 + 예상 질문 Q&A | 복습용 |

---

## 1. 30초 큰 그림

이 앱이 하는 일을 한 문장으로:

> **"달리는 사람의 심박수를 보고, 지금 '지방 연소에 최적인 강도(Zone 2)'인지 판단해 말로 코칭하고, 곧 강도를 벗어날 것 같으면 미리 알려준다."**

시작 전에 이 프로젝트의 제일 중요한 메시지 하나: **"AI = 신경망"이 아니다.** AI는 "데이터로 스스로 좋아지는 시스템"이고, 신경망(딥러닝)은 그 방법 중 하나일 뿐이다. 이 앱은 **문제마다 거기에 맞는 도구를 골라 썼다.** 어떤 문제엔 신경망이, 어떤 문제엔 베이지안이, 어떤 문제엔 아예 규칙(학습 없음)이 정답이다.

전체 시스템은 문제별 도구 3개 + 말하기(LLM) + 배관(파이프라인)으로 되어 있다:

```
워치(또는 GPS/가짜입력)          폰
┌──────────────┐    ┌──────────────────────────────────────────────────┐
│ 심박, 위치 수집 │──▶│ ①청소 ②규칙판정 ③특징 ④심박예측(NN) ⑤개인화 ⑥코칭 │──▶ 화면 + 음성
└──────────────┘    └──────────────────────────────────────────────────┘
```

| 문제 | 쓰는 도구 | 왜 이 도구인가 | 코드 |
|------|------|------|------|
| **가까운 미래 심박 예측** (30/60초 뒤) | **신경망(딥러닝)** | 1Hz로 계속 흘러드는 시계열 + 문헌 선례(RNN/LSTM). 선제 코칭/목표 페이스 제안에 쓴다 | `ml/train_hr_dynamics.py` → `app/.../HrDynamics.kt` |
| **이 사람의 Zone 2 범위** (몇 bpm?) | **온라인 베이지안** | 개인 데이터가 소수 + 정답(토크테스트)이 하나씩 온라인으로 들어옴 → 신경망이면 과적합. **이게 이 앱의 진짜 개인화 학습** | `ml/personalization.py` → `app/.../Personalization.kt` |
| **지금 Zone 2 안/밖?** (판정) | **규칙** | 경계가 정해지면 "범위 밖?"은 결정론. 학습할 게 없고, 규칙이 오히려 더 안전 | `app/.../ZoneJudge.kt` |
| **판정을 말로** | LLM(Gemini Nano) | 자연스러운 문장은 생성형 AI가 잘한다. 방향은 규칙이 잠근다 | `app/.../LlmCoach.kt` |

**왜 하나로 안 하고 나눴나?** 세 문제의 데이터 성격이 완전히 다르기 때문이다. 심박 예측은 데이터가 넘쳐서 신경망이 맞고, 개인 경계는 사람마다 정답이 몇 개뿐이라 베이지안이 맞고, 판정은 아예 학습이 필요 없다. **"모든 문제에 신경망"은 오히려 결함을 만든다** — 실제로 이 프로젝트는 판정에 신경망을 뒀다가 결함을 발견하고 규칙으로 바꿨다(§4-7, §11 Q7). "올바른 도구를 올바른 자리에" 고른 판단 자체가 이 앱의 AI다.

---

## 2. 기초 개념: 러닝과 심박 (이걸 모르면 뒤가 안 읽힘)

### 심박수(HR, Heart Rate)
1분에 심장이 뛰는 횟수. 단위 bpm. 쉴 때 50~70, 전력질주 때 180~200.

### Zone(존)이란
운동 강도를 심박 기준으로 5단계로 나눈 것. **Zone 2 = "옆사람과 대화 가능한 편안한 강도"**. 이 강도가 지방을 태우고 지구력 기초를 만드는 데 제일 효율적이라 러너들이 집착하는 구간이다.

### Zone 2 경계 공식 — 우리 앱의 "출발점" (%HRmax 기준)
```
최대심박(HRmax) ← 나이 공식(Tanaka: 208 - 0.7 x 나이)으로 추정  (예: 35세 → 약 183)
Zone2 상한 = 0.70 x HRmax   (예: 183 x 0.70 ≈ 128 bpm)  ← 70%HRmax ≈ LT1(젖산역치1) 근사
Zone2 하한 = 0.60 x HRmax   (예: 183 x 0.60 ≈ 110 bpm)
```
- 우리 기준은 **%HRmax**(최대심박의 몇 %)다. 상단 70%는 유산소 상한(LT1, 대화가 힘들어지기 시작하는 지점)의 근사치, 하단은 약 60%.
- (참고: 옛 버전은 여유심박 %HRR을 썼지만 지금은 %HRmax로 통일했다. 내부 특징 계산 시엔 HRR 비율로 환산해 정규화만 한다.)
- **문제**: 이 공식은 "평균적인 사람" 기준. 같은 나이라도 마라토너와 초보의 진짜 경계는 다르다. 개인은 이 경계에서 위아래로 벗어나 있다. → 그래서 **개인화**(§5)가 필요하고, 그게 이 앱에서 데이터로 진짜 학습하는 부분이다.

### 페이스(pace)와 케이던스(SPM)
- 페이스: 1km를 몇 분에 뛰나. min/km. 낮을수록 빠름. 속도(km/h)와 역수 관계: `페이스 = 60 / 속도`.
- 케이던스(SPM): 1분당 발걸음 수. 보통 160~180.

### 심박 지연(HR lag)과 심혈관 드리프트(Cardiac Drift) — **심박 예측 신경망이 필요한 이유**
- **지연**: 오르막을 만나 힘들어져도 심박은 20~40초 늦게 올라간다. 지금 심박만 보면 "곧 힘들어진다"를 놓친다.
- **드리프트**: 같은 속도로 계속 뛰어도 시간이 지나면(체온 상승, 탈수) 심박이 슬금슬금 오른다. 심박이 올랐다고 "강도가 높아진 것"이 아닐 수 있다.
- 그래서 이 앱은 두 가지를 나눈다. **"지금 안/밖인가?"(판정)는 규칙**으로 결정론적으로 답한다 — 이미 지속 심박이 경계 밖이면 그냥 밖이다(§4-7). **"30~60초 뒤엔 어떻게 될까?"(예측)는 신경망**에 맡긴다 — 심박+페이스+경사+피로의 복잡한 비선형 조합으로 미래를 내다봐야 하기 때문(§4). 예측이 "곧 넘어요"라고 하면 이탈 전에 미리 감속 코칭을 낸다(선제 코칭).

### 디커플링(decoupling)
"심박/페이스 비율"이 러닝 초반 대비 얼마나 커졌나(%). 드리프트를 숫자로 잡는 지표. 초반과 같은 페이스인데 심박만 5% 올랐으면 디커플링 5%. 지금 이 앱에서 디커플링은 (1) 화면/리포트 표시용 지표, (2) 개인화(§5)의 **보조** 관측(약한 신호)으로만 쓴다. 판정에도, 심박 예측 신경망의 입력에도 넣지 않는다(ablation 결과 예측엔 무익, §4-5).

---

## 3. 데이터의 여행 — 1초 동안 일어나는 일

앱이 켜져 있는 동안 **매초** 아래가 반복된다. 이 흐름이 곧 `RunEngine.kt`(오케스트레이터)다.

```
[1] 샘플 도착: Sample(시각, 심박, 페이스, 케이던스, 경사, 위도, 경도)
     ← 어디서? 셋 중 하나(§8): 워치 / 시뮬레이터 / 가짜입력(Mock)
[2] 청소(OutlierGuard): 심박이 40~220 밖이면 "고장난 값"으로 버리고 직전 값 유지
[3] 개인 경계 읽기: 개인화(§5)가 학습한 Zone2 하한/상한(bpm)을 가져온다
[4] 판정(ZoneJudge = 규칙): 지속 심박(최근 60초 평균) vs 개인 경계 → "미달 / 유지 / 초과"
     + 히스테리시스(경계 ±2bpm 확실히 넘을 때만 전환)로 경계에서 깜빡임 방지. 신경망 안 씀
[5] 특징 만들기(FeatureExtractor): 최근 기록으로 예측용 특징 7개 계산 (§4-3). 판정엔 안 쓰고 예측/개인화에만
[6] 심박 예측(HrDynamics = 신경망): 특징 7개 → "현재 페이스 유지 시 30/60초 뒤 심박" 예측 (§4)
     → Zone2 목표 페이스 역질의 + "곧 넘어요" 선제 코칭. (모델 없으면 예측만 생략, 판정/앱은 계속)
[7] 개인화 관측 수집(Personalization): "이 사람의 진짜 경계" 단서(토크테스트=주 라벨, 디커플링=보조)를
     모아 경계 추정치를 갱신 (§5)
[8] 코칭(Coach): 판정이 바뀌었거나 선제 트리거가 뜨고 20초 지났으면 → 방향 결정(규칙) → 문장 생성(LLM) → TTS 음성
[9] 기록: 경로 점, 심박/페이스 시계열, 존 체류 시간을 누적 → 종료 시 리포트/저장
```

> 코드로 따라가기: `app/app/src/main/java/com/zone2runner/app/pipeline/RunEngine.kt`의 `onSample()` 함수가 위 [2]~[9]를 순서대로 한다. 직접 읽어보자. 이 파일 하나가 이 프로젝트의 "척추"이고, 세 도구(규칙 판정 / 심박 예측 NN / 베이지안 개인화)가 어떻게 협력하는지가 한눈에 보인다.

---

## 4. AI ① — 심박 예측 신경망 (딥러닝, 제일 중요한 챕터)

이 앱에서 **딥러닝(신경망)을 진짜로 설계/학습/배포한 유일한 부품**이 이거다. 하는 일은 "판정"이 아니라 **가까운 미래 심박 예측**이다: *"지금 이 페이스를 유지하면 30초 뒤, 60초 뒤 심박이 몇이 될까?"* 이걸 알면 (1) 이탈하기 전에 미리 코칭하고(선제 코칭), (2) "Zone 2에 딱 머무는 페이스"를 거꾸로 찾아 제안할 수 있다. 코드: `app/.../pipeline/HrDynamics.kt`, 학습: `ml/train_hr_dynamics.py`, 명세: `spec/spec-014`.

### 4-1. 신경망이 뭔가: "곱하고, 더하고, 꺾는다"

신경망의 최소 단위(뉴런)가 하는 일은 딱 세 가지다:

```
출력 = 꺾기( 입력1 x 가중치1 + 입력2 x 가중치2 + ... + 보정값 )
```

1. 입력마다 **가중치(weight)를 곱하고**
2. 전부 **더하고**(+보정값 bias)
3. 결과가 음수면 0으로 **꺾는다**(ReLU라고 부름. "음수는 버려"가 전부)

이게 전부다. "학습"이란 **가중치 숫자들을 정답에 맞게 조금씩 조정하는 과정**일 뿐이다.

뉴런 하나는 직선 하나밖에 못 긋지만, 수십 개를 층층이 쌓으면 구불구불한 관계(비선형)를 그릴 수 있다. 우리가 필요한 게 바로 그것 — "오르막이면서 심박이 아직 낮지만 상승 중이고 세션 후반(피로)이면 → 60초 뒤 심박은 꽤 오른다" 같은 **여러 조건이 얽힌 복잡한 관계로부터 미래 심박을 뽑아내는 것**.

### 4-2. 우리 신경망의 크기

```
입력 7개 → [1층: 뉴런 32개] → [2층: 뉴런 16개] → 출력 2개(30초 뒤 심박, 60초 뒤 심박)
```
- 출력이 **2개의 숫자**(각 시점의 예측 심박)다. 판정처럼 "셋 중 하나 고르기(분류)"가 아니라 **수를 맞히는 회귀(regression)**다. 그래서 softmax도, argmax도 없다.
- 가중치 개수: 7x32 + 32x16 + 16x2 + 보정값 ≈ **약 800개의 숫자**. 이게 모델의 전부다.
- ChatGPT류가 수천억 개인 것과 비교하면 초소형. 그래서 폰에서 1ms도 안 걸리고, 별도 AI 엔진 없이 Kotlin 반복문으로 돌린다(§4-6, adr-011).

### 4-3. 입력 7가지 — 신경망에게 주는 "단서들"

신경망은 원시 심박을 그대로 받지 않는다. **사람이 설계한 의미 있는 숫자 7개**(특징, feature)로 가공해 준다. `spec-014` 순서 그대로:

| # | 이름 | 뜻 | 왜 주나 |
|:-:|------|------|------|
| 0 | `hr_now_frac` | 최근 10초 평균 심박(HRR 비율로 정규화) | 지금 심박 수준 |
| 1 | `hr_sus_frac` | 최근 60초 평균 심박(지속 심박) | 흔들림을 뺀 현재 강도 |
| 2 | `dHR` | 최근 30초간 심박 상승 속도(bpm/초) | 심박 지연 반영 — "지금은 낮아도 오르는 중" |
| 3 | `pace_plan` | **앞으로 유지할 페이스**(min/km) | "이 페이스로 가면?"의 핵심 조건. 이 값을 바꿔 끼워 what-if 예측 |
| 4 | `slope` | 경사(%) | 오르막이면 같은 페이스라도 심박이 더 오른다 |
| 5 | `spm` | 케이던스 | 뛰는 리듬 정보 |
| 6 | `elapsed_min` | 세션 경과 시간(분) | 피로 대리 — 오래 뛸수록 드리프트로 심박이 오른다 |

**포인트 1**: 심박을 개인 프로필(안정심박/HRR)로 정규화한 비율로 넣는다. 그래야 "다양한 러너"에게 두루 통하는 모델이 된다.
**포인트 2**: 3번 `pace_plan`이 "만약 이 페이스를 유지하면"이라는 **가정(what-if) 조건**이다. 이 슬롯만 여러 후보 값으로 바꿔가며 예측하면(§4-6의 `recommendPace`) "Zone 2에 딱 머무는 페이스"를 거꾸로 찾을 수 있다.

> **디커플링(드리프트)은 왜 입력에 없나?** 초기엔 8번째 특징으로 넣었다가, ablation 실험(`ml/ablation_decoupling.py`)에서 **예측 정확도엔 오히려 방해**임을 확인해 뺐다(§4-5). 대신 그 자리를 "학습 단계 단조성 페널티"로 대체했다. 이게 "무작정 특징을 다 넣지 않고, 실험으로 골라냈다"는 설계 근거다.

### 4-4. 학습 데이터: 정답(라벨)은 어디서 났나 — 그리고 예전의 함정

**판정 신경망을 죽인 교훈을 먼저 알아야 한다.** 예전엔 신경망에게 "미달/유지/초과"를 직접 맞히게 했다. 그런데 그 정답 라벨을 "지금 강도가 경계보다 위/아래냐"로 만들었더니, **심박이 급강하하면 '곧 존 아래로 감'이 라벨과 상관돼서, 모델이 "추세만 보고 현재 위치를 무시하는 지름길"을 배웠다**(라벨 순환). 실기기에서 "심박이 상한을 42bpm이나 넘었는데 판정=미달"이 나왔다. 정답을 규칙으로 "정의"하면 신경망이 그 규칙의 허점을 파고든다.

**지금 심박 예측 모델은 이 함정을 피한다.** 정답 라벨 = **실제로 관측된 미래 심박**이다. 시뮬레이터가 라벨을 "정의"하지 않고 "관측"만 준다.

1. 가상 러너 150명 생성(나이, 안정심박, 최대심박 랜덤). 각 러너가 30분씩 8세션을 "뛴다". 물리 법칙(심박 지연, 드리프트, 노이즈, §7)으로 심박 곡선이 만들어진다.
2. 매 순간 t에서: **입력** = 그 시점 특징 7개, **정답** = 시뮬 안에서 t+30초, t+60초에 **실제로 나온 심박**(각 시점 ±5초 평균으로 살짝 잡음 완화).
3. 결과: **약 39만 개**(러너 150 x 세션 8)의 "특징 → 진짜 미래 심박" 짝.

정답이 "실제 미래값"이라 순환이 원천 차단된다. 남는 리스크는 **"시뮬레이터가 현실과 얼마나 닮았나"**인데, 물리 모델(지연/드리프트)을 문헌 기반으로 넣어 완화하고, 향후 실주행 로그로 미세조정(fine-tune)하는 것을 과제로 남겼다. 라벨이 "관측값"이라 실데이터가 쌓이면 추가 라벨링 없이 같은 파이프라인으로 재학습된다.

### 4-5. 학습 과정: 신경망을 가르치는 법 (`ml/train_hr_dynamics.py`)

```
반복 (early stopping까지):
  1. 학습 데이터에서 한 묶음씩 꺼낸다
  2. 현재 가중치로 30/60초 뒤 심박을 예측해 본다 (순전파)
  3. 정답(실제 미래 심박)과 얼마나 벌어졌나 점수를 낸다 (손실, MSE = 오차 제곱 평균)
     + 페이스 단조 페널티: "느리게 뛰는데 예측 심박이 오르면" 벌점 (아래 설명)
  4. 각 가중치를 "어느 쪽으로 움직이면 덜 틀릴까" 계산해(역전파) 아주 조금 움직인다 (Adam)
  5. 검증 러너들 성적이 더 안 오르면 중단 (early stopping)
```

- **러너 단위로 train/검증/시험 분리**: 같은 러너 데이터가 학습과 시험에 둘 다 들어가면 "그 사람을 외운" 컨닝이 된다. 러너 자체를 갈라 "처음 보는 사람에게도 되는지"를 시험한다.
- **페이스 단조 페널티**: 역질의(페이스 제안)가 성립하려면 "느리게 뛰면 예측 심박이 내려가야" 한다. 학습 중 페이스 입력을 살짝 흔들어 예측이 거꾸로 오르면 벌점을 줘서 이 상식을 강제한다. 디커플링 특징을 뺐을 때 깨지던 단조성(위반 18%)을 이 페널티로 0%까지 되돌렸다.

**결과 성적표** (`ml/EXPERIMENT_LOG.md`) — 정직하게, "아무것도 학습 안 한 단순 기준선(baseline)"과 비교한다:

| 지평 | 우리 모델 RMSE | B0 지속(persistence): "지금 심박 그대로" | AC2(둘 다 하회?) |
|------|---:|---:|:---:|
| 30초 뒤 | **14.01 bpm** | 17.01 bpm | O |
| 60초 뒤 | **20.45 bpm** | 30.23 bpm | O |

- RMSE = "평균적으로 몇 bpm 빗나갔나"(낮을수록 좋음). 우리 모델이 "지금 심박이 그대로 유지될 것"이라는 순진한 기준선보다 두 시점 모두 확실히 낫다.
- 페이스 응답 단조성 위반율 **0.0%**(목표 5% 미만) — 역질의(목표 페이스 제안)가 믿을 만하다는 근거.
- (참고: 예전 "판정 신경망"의 82.6% 같은 정확도 수치는 §4-4의 라벨 순환으로 **자기참조 채점**이라 폐기됐다. 지금 지표는 "실제 미래 심박을 몇 bpm 맞혔나"라는 정직한 회귀 오차다.)

### 4-6. 학습은 파이썬인데 폰은 Kotlin — 모델을 어떻게 옮겼나

여기가 재밌는 부분. 흐름:

```
[학습] train_hr_dynamics.py로 학습 + 성능 확인
[JSON 추출] 가중치 약 800개 + 정규화 상수를 텍스트 파일로 (assets/hr_dynamics.json)
[폰에서 로드] Kotlin이 JSON 읽어서 곱하기/더하기 직접 수행 (HrDynamics.kt)
```

**왜 TFLite(구글의 모바일 AI 엔진)를 안 썼나?** (adr-011) 모델이 겨우 800개 숫자라서. 몇 MB짜리 엔진을 붙이는 건 "종이비행기 접으려고 공작기계 사는 격". 대신 추론 코드를 직접 짰다 — 그래서 **AI가 어떻게 계산하는지 코드에 그대로 보인다**. 직접 보자:

```kotlin
// app/.../pipeline/HrDynamics.kt — predictFrac() 핵심부 (실제 코드)
var x = DoubleArray(feat.size) { (feat[it] - mean[it]) / scale[it] }  // ① 표준화
for ((idx, layer) in layers.withIndex()) {
    val out = DoubleArray(layer.b.size)
    for (o in out.indices) {                 // ② 각 뉴런마다:
        var s = layer.b[o]                   //    보정값에서 시작
        val row = layer.w[o]
        for (i in x.indices) s += row[i] * x[i]  //    곱하고 더한다
        out[o] = s
    }
    if (idx < layers.size - 1)               // ③ 마지막 층 빼고
        for (o in out.indices) if (out[o] < 0) out[o] = 0.0  //    음수는 0으로(ReLU)
    x = out
}
// ④ 마지막 x = 예측값 2개 [30초 뒤 심박비율, 60초 뒤 심박비율] — 그대로 씀(분류가 아니라 회귀)
```

- ① 표준화: 특징마다 크기가 다르니(페이스는 6쯤, spm은 170쯤) "평균 빼고 표준편차로 나눠" 비슷한 크기로 맞춘다. 학습 때와 **같은** 상수를 써야 해서 JSON에 함께 저장돼 있다.
- ②~③: §4-1에서 말한 "곱하고 더하고 꺾기" 그대로. 신경망의 실체가 이중 for문이라는 것.
- ④ **판정 신경망과 결정적 차이**: 마지막 출력을 softmax/argmax로 "고르지" 않고, **그 수 자체를 예측 심박으로 쓴다.** 회귀란 이런 것.

**이걸로 뭘 하나 (`recommendPace`)**: `pace_plan` 슬롯만 4.0~12.0 min/km 사이 후보로 갈아끼우며 60초 뒤 심박을 반복 예측하고, **예측이 Zone 2 밴드 한가운데에 가장 가까운 페이스**를 골라 "목표 페이스"로 제안한다. 예측 모델을 "거꾸로" 쓰는 것(역질의). 또 지금 페이스 유지 시 60초 뒤 예측이 상한+2bpm을 넘으면, 아직 IN이어도 "곧 넘어요" **선제 코칭**을 낸다(spec-014 FR4).

**검증**: 이 Kotlin 코드가 파이썬 학습 모델과 같은 답을 내는지 단위 테스트로 확인했다(`HrDynamicsTest.kt` — 실제 JSON 로드, 예측이 생리 범위 안인지 + 페이스 스윕 방향성 확인).

### 4-7. 판정은 왜 신경망이 아니라 규칙인가 (`ZoneJudge.kt`)

"지금 Zone 2 안/밖인가?"(판정)는 일부러 신경망을 **안** 썼다. 규칙이다:

```
지속 심박(최근 60초 평균)  vs  개인 경계[하한, 상한]  (개인화 §5가 학습한 값)
   경계보다 낮으면 → 미달 / 사이면 → 유지 / 높으면 → 초과
   + 히스테리시스: 새 상태 방향으로 경계 ±2bpm을 확실히 넘을 때만 전환 (경계에서 깜빡임 방지)
```

왜 규칙이 정답인가:
- **결정론이면 충분**: 경계가 정해지면 "범위 밖?"은 그냥 큰지 작은지 비교다. 학습할 파라미터가 없다.
- **모순이 구조적으로 불가능**: 화면 밴드와 판정이 **같은 경계**를 본다. "밴드는 초록인데 판정은 초과" 같은 모순이 원천 차단된다. 신경망 판정은 이걸 보장 못 한다.
- **§4-4의 라벨 순환 결함**: 신경망을 판정에 뒀다가 "42bpm 초과인데 미달" 오판을 겪고 규칙으로 옮겼다. 판정은 규칙이 더 안전하다는 걸 실패로 배웠다.

> **한 줄 정리(도구 선택 리즈닝)**: 무작정 신경망을 쓴 게 아니라 — **미래를 내다보는 비선형 예측엔 신경망(§4)**, **경계 대비 크다/작다 판정엔 규칙(§4-7)**, **소수 데이터 개인화엔 베이지안(§5)**, **문장 표현엔 LLM(§6)**을 골랐다. "올바른 도구를 올바른 자리에" 고른 판단이 이 앱의 AI다(adr-016).

---

## 5. AI ② — 베이지안 개인화 (신경망 아님, 그런데 이게 이 앱의 진짜 "학습"이다)

> 오해 주의: 딥러닝(§4)은 "심박 예측"이라는 별개 문제를 푼다. **"이 사람의 Zone 2를 데이터로 알아가는" 진짜 개인화 학습은 여기, 베이지안이다.** 신경망이 아니라고 "학습이 아닌" 게 아니다 — 데이터가 쌓일수록 추정이 정확해지면 그게 학습(온라인 ML)이다.

### 5-1. 푸는 문제

공식 Zone 2 상한이 128인데 진짜는 138인 사람이 있다(엘리트일수록 위로 벗어난다). 공식대로면 이 사람에게 "느리게!"를 10 bpm이나 일찍 외친다. **개인의 진짜 경계를 어떻게 알아낼까? 그것도 실험실(젖산 검사) 없이, 몇 번의 러닝 데이터만으로?**

### 5-2. 베이지안 사고방식 — 몸무게 재기 비유

집에 부정확한 체중계가 있다 치자.
- **사전(prior)**: "나는 대충 70kg일 것" (지난달 병원 기록) — 확신도 중간
- **관측(evidence)**: 체중계가 73kg을 가리킴 — 근데 이 체중계는 오차가 큼
- **사후(posterior)**: "그럼 71kg쯤이겠네" — **둘 사이, 그런데 더 믿을 만한 쪽에 가깝게**

핵심 규칙 두 개:
1. 새 정보가 올 때마다 믿음을 **"확신도에 비례해 가중평균"으로 갱신**한다.
2. 갱신할수록 **불확실성(σ)이 줄어든다** — 정보가 쌓이니 당연.

### 5-3. 우리 코드에 그대로 있다 (`Personalization.kt`, 15줄)

**관측(정답 단서)은 두 가지**, 확신도(obsSd)가 다르다:
- **토크테스트 = 주 라벨**(강한 관측, adr-016): 러닝 중 "지금 대화 체감"을 **5단계**(아주 편함/편함/애매/벅참/매우 벅참, spec-016)로 답하면, 그 순간 심박이 경계 대비 어디였는지 단서가 된다. 각 단계를 서로 다른 관측(z, σ)으로 매핑: 중앙 **애매(≈VT1)**는 지금 심박을 경계로 직접 관측(σ=6, 최고 확신), 편함/벅참은 ±5bpm(σ=14), 극단(아주 편함/매우 벅참)은 ±10bpm(σ=16, 거리 불확실해 넓게). 참값(젖산 검사)에 가장 가까운 라벨이라 확신도를 높게 준다.
- **디커플링 = 보조 라벨**(약한 관측): 사용자 입력 없이도 드리프트가 임계 부근일 때의 지속 심박을 약한 신호로 5분마다 반영. 편향이 있어 확신도(obsSd)를 크게(약하게) 준다.

```kotlin
// 사전: 공식 상한(%HRmax 0.70 → 예: 128 bpm), 불확실성 σ
// 관측: 토크테스트(강함) 또는 디커플링(약함) — obsSd로 확신도를 조절
fun update(zBpm: Double, obsSd: Double) {
    val p0 = 1.0 / variance          // 내 기존 믿음의 확신도 (분산의 역수 = 정밀도)
    val p1 = 1.0 / (obsSd * obsSd)   // 관측의 확신도 (토크테스트는 작게=강하게, 디커플링은 크게=약하게)
    muUpper = (muUpper * p0 + z * p1) / (p0 + p1)  // ← 확신도 가중평균. 이 한 줄이 베이지안 전부
    variance = 1.0 / (p0 + p1)       // 불확실성은 반드시 감소
    muUpper = muUpper.coerceIn(mu0 - 10, mu0 + 10) // 안전 가드: 세션 내 ±10 bpm 이상 못 벗어남
}
```

숫자로 따라가 보자. 사전 128(σ=8), 토크테스트 관측 138(obsSd=10):
```
p0 = 1/64 = 0.0156,  p1 = 1/100 = 0.01
새 추정 = (128x0.0156 + 138x0.01) / 0.0256 ≈ 131.9
새 σ = √(1/0.0256) ≈ 6.25   ← 8에서 줄었다
```
관측이 쌓일수록 128 → 131.9 → 134 → ... 진짜 경계(138)로 **수렴**하고 σ는 계속 준다. **세션이 끝나면 최종 경계를 `LearnedZone`에 저장했다가 다음 세션의 사전(prior)으로 쓴다** — 이게 "세션을 거듭할수록 나에게 맞춰지는" 온라인 학습의 실체다(QA3 적응성). ml 실험에서 세션 누적 시 오차 감소를 실증했다.

### 5-4. 왜 여기엔 신경망을 안 썼나 (설계 판단, adr-016의 네 가지 근거)

같은 앱에서 심박 예측엔 신경망을 썼는데(§4) 여기엔 안 썼다. 문제의 데이터 성격이 정반대라서다:

- **개인별 라벨이 소수** → 신경망은 데이터 부족으로 과적합. (심박 예측은 1Hz로 데이터가 넘쳐서 반대)
- **정답이 하나씩 온라인으로 들어온다**(토크테스트를 러닝 중 그때그때) → 대규모 재학습이 아니라 **한 관측마다 즉시 갱신**하는 게 맞다. 베이지안 update() 한 줄이 딱 그것.
- **콜드스타트가 필요**하다 → 베이지안은 사전(공식)이 있어 **데이터 0개인 신규 사용자도** 합리적으로 시작.
- **불확실성을 스스로 다뤄야** 한다(σ) → "아직 자신 없음"을 표현 가능. 신경망은 점 추정만 낸다.
- 결론: **"AI = 신경망"이 아니다.** 데이터가 소수/온라인일 땐 베이지안이, 연속/다수일 땐(심박 예측) 신경망이 맞는 도구다. 이 판단 자체가 이 프로젝트의 핵심 어필 포인트다.

### 5-5. 정직한 한계

관측(디커플링 임계 부근 심박 추출)이 편향되면 엉뚱한 곳으로 수렴한다. 실제 실험에서 임계추출(Conconi 방식)의 편향 문제를 확인했고, **"관측이 좋으면 수렴한다(메커니즘 검증), 좋은 관측 만들기는 향후 과제"**로 문서화했다(HANDOFF §5). 한계를 아는 것도 설계 능력.

또 하나: **토크테스트가 주관 자기보고**라 재현성이 약하다. 이를 객관 신호로 바꾸려고 별도 PoC(`voice-poc`)에서 세 가지를 실기기 검증했다 — ① 음향 타이밍(완독시간/끊김), ② 온디바이스 ASR 완성도, ③ **YAMNet(AudioSet) 호흡 감지**. 결과는 **모두 온디바이스 신뢰 불가**: 특히 YAMNet은 폰 마이크 날숨을 "Beatboxing/Music"으로 오분류했고, 문헌([호흡률 논문 PMC10296589])도 러닝 소음 때문에 마스크 마이크가 필요하다고 보고한다. 그래서 **주관 5단계 설문이 무비용 견고한 최적해**로 남았다(adr-018 기각). 객관화를 위해 ML/ASR/DSP를 다 시도하고 데이터로 부적합을 확인한 뒤 주관으로 회귀한 것 — 이 판단 자체가 adr-016 "문제에 맞는 도구" 원칙의 실증이다.

---

## 6. AI ③ — LLM 코칭 (Gemini Nano)

### 6-1. LLM이 뭔가, 그리고 "온디바이스"가 왜 중요한가

LLM = 문장을 만들어 내는 생성형 AI(ChatGPT류). 우리는 **Gemini Nano** — 구글이 폰 안에 넣어둔 소형 LLM — 를 쓴다. 인터넷 없이 폰 칩으로만 동작(온디바이스). 러닝은 밖에서 하니까 오프라인 동작이 필수 + 심박은 민감정보라 폰 밖으로 안 내보내는 게 안전. 실기기(S26)에서 동작 확인 완료(adr-007, `llm-verify/`).

### 6-2. 설계의 핵심: "방향은 규칙이, 말투만 LLM이" (adr-002)

LLM의 약점 = 가끔 헛소리(환각). 운동 코칭에서 "빨리 뛰세요"를 반대로 말하면 위험하다. 그래서:

```
판정(초과) → 규칙이 방향 확정: "느리게 하라고 말할 것"   ← AI가 못 바꾸는 부분
           → LLM에게: "이 방향으로, 오르막 상황 반영해서, 25자 내외 한 문장만" (방향 잠금 프롬프트)
           → 출력 가드: 길이 제한, 따옴표 제거, 첫 문장만   ← 이상한 출력 차단
           → 실패하면? 미리 써둔 규칙 문장으로 폴백. 코칭이 끊기는 일은 없음
```

`LlmCoach.kt`의 `buildPrompt()`와 `guard()`를 열어보면 위가 코드 그대로 있다. **"생성형 AI를 안전하게 쓰는 표준 패턴"**(방향 잠금 + 출력 가드 + 폴백)이라 다른 프로젝트에도 써먹을 수 있는 지식.

---

## 7. 시뮬레이터 — 학습 데이터 공장 (`ml/simulator.py`, `app/.../RunSimulator.kt`)

가상 러너가 "물리적으로 그럴듯하게" 뛰게 만드는 부품. 신경망 학습 데이터(§4-4)가 여기서 나오고, 앱의 시뮬레이션 모드도 이것의 Kotlin 포팅이다. 넣은 물리 3가지:

1. **심박 지연**: 목표 강도로 심박이 **서서히** 따라감. `hr[i] = hr[i-1] + (목표 - hr[i-1]) / τ` — τ(타우, 20~40초)가 클수록 느린 심장. "지수 이동"이라는 고전 물리 패턴.
2. **드리프트**: 경계보다 높은 강도로 뛰는 동안 심박이 추가로 슬금슬금 상승(누적 피로 모사).
3. **노이즈**: 광학 센서 오차를 가우시안 잡음으로.

여기에 지형(오르막/내리막 구간)과 강도 변화(인터벌)를 랜덤 배치. **러너마다 파라미터(경계, τ, 드리프트 정도, 노이즈)가 달라서** 신경망이 "다양한 사람"을 경험하게 된다.

> 재미: 앱의 "가짜 라이브(Mock)" 모드(§8)는 이것보다 단순한 실시간 버전 — 심박/속도가 지정 범위 안에서 랜덤워크하고 GPS가 실제로 이동한다.

---

## 8. 앱 구조 — AI를 담는 그릇

### 8-1. 화면 6개 (폰, `app/`)

```
Home ──러닝 시작──▶ Run(라이브: 지도+심박+판정+코칭) ──종료──▶ Report(차트+분석+경로)
 ├─ History(기록 목록 → 리포트 다시 보기)
 ├─ Profile(나이/안정심박 → Zone2 목표 계산)
 └─ MockConfig(가짜 라이브 설정: 심박/속도 범위)
```

### 8-2. 이 프로젝트에서 제일 자랑할 만한 앱 설계: RunSource 추상화

파이프라인(§3)은 "1초에 하나씩 Sample을 받는다"는 것만 안다. **누가 주는지 모른다.** 주는 쪽은 3종:

| 소스 | 무엇 | 언제 쓰나 |
|------|------|------|
| `SimulatedRunSource` | 시뮬 세션 70배속 재생 | 데모, 파이프라인 점검 |
| `LiveRunSource` | 진짜 GPS + 워치 심박(블루투스 Data Layer) | 실제 러닝 |
| `MockRunSource` | 심박/속도 범위 지정 실시간 합성 | 워치 없이 개발/QA/시연 |

이렇게 만들면: 워치가 없어도 전체를 검증할 수 있다(= QA5 "테스트 가능성" 달성). 나중에 새 센서가 나와도 소스 하나만 추가하면 된다. **"인터페이스에 의존하고 구현에 의존하지 마라"** 라는 소프트웨어 설계 격언의 실물 예제.

### 8-3. 저장

- 세션: 러닝 1회 = JSON 파일 1개(`filesDir/sessions/`). DB 없이. 규모가 작을 땐 단순한 게 정의.
- 프로필/Mock 설정: SharedPreferences(안드로이드 기본 키-값 저장소).

### 8-4. 워치(`wear/`)

수집+표시 전담(판정/예측/개인화는 폰 — adr-001의 역할 분담). 심박을 측정해 화면에 존 게이지로 보여주면서, 동시에 폰으로 `/hr` 메시지 전송(`HrForwarder`). 워치의 존 표시는 %HRmax 5구간 경량판 — 개인 경계 기반 규칙 판정(ZoneJudge)과 심박 예측(HrDynamics)은 폰이 한다는 이중 구조.

---

## 9. 직접 해보기 (실습 — 이걸 해야 진짜 내 것이 됨)

**준비**: `app/` 폴더에서 실행. JAVA_HOME은 Android Studio JBR.

1. **테스트 돌려보기** (5분): `./gradlew.bat testDebugUnitTest` → 전체 통과(40여 건) 확인. 그다음 `app/app/src/test/.../HrDynamicsTest.kt`를 열어 "실제 모델 JSON을 읽어 심박을 예측"하는 테스트를, `ZoneJudgeTest.kt`를 열어 "규칙 판정이 밴드와 모순되지 않음"을 확인하는 테스트를 읽는다.
2. **신경망 손계산 체험** (15분): `HrDynamicsTest.kt`의 `forwardPass_linearModel_isExact`에 있는 초미니 모델(입력 2, 출력 2)을 종이에 그려 §4-6 코드를 손으로 따라 계산해 본다. 출력을 argmax로 "고르지" 않고 **그 수를 그대로 예측값으로 쓰는 회귀**임을 체감하는 게 목적.
3. **모델 성적 재현** (10분): `python ml/train_hr_dynamics.py` 실행 → 모델 RMSE(30초 14.01 / 60초 20.45) vs 기준선(persistence 17.01 / 30.23), 페이스 단조성 위반율 0.0%가 출력되는 걸 직접 본다. (`.venv` 필요: HANDOFF 참고)
4. **모델 구조 바꿔보기** (30분): `train_hr_dynamics.py`의 은닉층 크기(32, 16)를 (8,)로 줄여 재실행 → RMSE가 어떻게 변하나? (64, 32)로 늘리면? "모델 크기 vs 성능" 감각이 생긴다.
5. **디커플링 ablation 체험** (15분): `python ml/ablation_decoupling.py` 실행 → 디커플링 특징을 넣고/빼고 단조 페널티를 켜고 껐을 때 RMSE와 페이스 단조성이 어떻게 갈리는지 본다(§4-3, §4-5의 표를 직접 재현).
6. **베이지안 손계산** (15분): §5-3의 숫자 예제를 계산기로 두 번 반복해 보고, `PipelineTest.kt`의 `personalization_movesTowardObservationAndStaysBounded`와 비교. "관측마다 추정이 그쪽으로 조금씩 이동 + σ 감소"를 눈으로 확인.
7. **Mock 모드로 검증** (폰 있으면, 20분): 앱 설치 → 가짜 라이브 → 심박 범위를 일부러 "초과" 수준(160~180)으로 → 앱이 "느리게" 코칭하는지, 대시보드에 60초 뒤 예측 심박/추천 페이스가 뜨는지, 리포트 존 분포가 주황(초과) 위주인지 확인. **규칙 판정 + 심박 예측을 눈으로 검증하는 것.**

---

## 10. 용어 미니 사전 (모르는 말 나오면 여기)

| 용어 | 한 줄 설명 |
|------|------|
| MLP | 가장 기본형 신경망. 층층이 "곱하고 더하고 꺾기". 우리 심박 예측 모델(§4)이 이것 |
| 회귀(regression) | 수를 맞히는 것(예: 60초 뒤 심박 = 몇 bpm). 우리 심박 예측 모델이 이 방식 |
| 분류(classification) | 몇 개 중 하나 고르기(예: 미달/유지/초과). 옛 판정 MLP가 이 방식이었으나 폐기 |
| 규칙(rule) | 학습 없이 정한 조건으로 결정. 우리 Zone2 판정(ZoneJudge)이 이 방식 |
| 히스테리시스 | 경계에서 판정이 깜빡이지 않게, 확실히 넘을 때만 상태를 바꾸는 완충장치 |
| RMSE | 예측이 평균 몇 bpm 빗나갔나. 회귀 성적표. 낮을수록 좋음 |
| persistence(지속) 기준선 | "지금 값이 그대로 유지될 것"이라 찍는 순진한 baseline. 모델은 이걸 이겨야 의미 |
| 온라인 학습 | 데이터가 하나씩 올 때마다 즉시 조금씩 갱신하는 학습. 베이지안 개인화(§5)가 이것 |
| 특징(feature) | 원시 데이터를 모델이 먹기 좋게 가공한 숫자 |
| 라벨(label) | 학습용 정답 |
| 순전파 | 입력→출력 방향으로 계산하는 것(=추론) |
| 역전파 | 틀린 만큼 가중치를 거꾸로 조정하는 학습 알고리즘 |
| 손실(loss) | "얼마나 틀렸나" 점수. 학습은 이걸 낮추는 게임 |
| 에폭(epoch) | 학습 데이터 전체를 1바퀴 도는 단위 |
| 과적합 | 문제를 암기해서 새 문제를 못 푸는 상태 |
| early stopping | 검증 성적이 안 오르면 학습 중단(과적합 방지) |
| ReLU | 음수를 0으로 꺾는 함수 |
| softmax | 점수들을 합이 1인 확률처럼 변환 |
| StandardScaler | 평균 0, 표준편차 1로 입력 크기 통일 |
| prior/posterior | (베이지안) 갱신 전 믿음 / 갱신 후 믿음 |
| σ(시그마) | 불확실성의 크기. 작을수록 확신 |
| 온디바이스 | 서버 없이 기기 안에서 AI 실행 |
| 폴백(fallback) | 주 방법 실패 시 예비 방법으로 전환 |
| TTS | 글자 → 음성 변환 |
| Data Layer | 워치↔폰 블루투스 메시지 통로(구글 API) |
| QA1~QA5 | 우리 품질 목표(spec-002): 정확성/강건성/적응성/효율성/테스트가능성 |

---

## 11. 예상 질문 Q&A (발표/심사 대비 — 소리 내어 답해보기)

**Q1. 신경망을 어디에 왜 썼나? 판정에 쓴 거 아닌가?**
A. 아니다. 신경망(딥러닝)은 **"가까운 미래 심박 예측"**(30/60초 뒤)에 썼다. 판정이 아니라 예측인 이유: 심박은 지연/드리프트/경사/피로가 얽혀 **비선형으로 흐르고**, 그 미래값을 내다봐야 이탈 전에 선제 코칭하고 목표 페이스를 제안할 수 있기 때문이다. 정직한 지표로 "지금 심박 유지" 기준선(persistence)보다 RMSE가 두 시점 모두 낮다(30초 14.01 vs 17.01, 60초 20.45 vs 30.23). 1Hz로 데이터가 넘치는 연속 시계열이라 신경망이 맞는 자리이고, 문헌(Zhu 2025 RNN, athlete_hr_predict LSTM) 선례도 있다.

**Q2. 개인화에 왜 딥러닝을 안 썼나?**
A. 데이터 성격이 정반대라서다. 개인별 정답 라벨(토크테스트)이 **소수이고 하나씩 온라인으로** 들어온다 → 신경망이면 과적합하고 재학습도 부적합하다. 베이지안은 공식을 사전분포로 써서 **데이터 0개(콜드스타트)부터 동작**하고, 관측마다 즉시 갱신되며, 쌓일수록 개인 경계로 수렴하고, 불확실성(σ)을 스스로 표현한다. **이게 이 앱의 진짜 개인화 학습**이다(adr-016).

**Q3. 그럼 판정(지금 Zone2 안/밖)은 뭘로 하나?**
A. **규칙**이다(ZoneJudge). 개인화가 학습한 경계와 지속 심박을 비교하고 히스테리시스로 깜빡임만 막는다. 경계가 정해지면 "범위 밖?"은 결정론이라 학습할 게 없고, 화면 밴드와 같은 경계를 봐서 모순이 구조적으로 불가능하다. **AI를 안 쓰는 것도 설계 판단**이다.

**Q4. 예전엔 신경망이 판정을 했다던데, 왜 바꿨나?** (핵심 질문)
A. 처음엔 미달/유지/초과를 신경망이 3분류로 맞히게 했다. 그런데 정답 라벨을 "지금 강도가 경계 위/아래냐"라는 **규칙으로 정의**했더니, 심박 급강하가 라벨과 상관돼 모델이 **"추세만 보고 현재 위치를 무시하는 지름길"**을 배웠다(라벨 순환). 실기기에서 "상한 42bpm 초과인데 미달" 오판이 나왔다. 그래서 판정은 규칙으로 옮기고, 신경망은 라벨 순환이 없는(정답=실제 미래 심박) **심박 예측**으로 재조준했다. 실패에서 배운 역할 재분리가 곧 설계다(adr-013, adr-016).

**Q5. 심박 예측 성능은 어떻게 채점했나? 82.6% 같은 정확도는?**
A. 그 82.6%는 폐기된 판정 MLP의 수치이고, 자기 라벨로 채점한 자기참조라 의미가 없었다. 지금은 회귀라서 **RMSE(실제 미래 심박을 평균 몇 bpm 맞혔나)**로 정직하게 채점한다: 30초 14.01 / 60초 20.45 bpm으로, 순진한 기준선(persistence 17.01 / 30.23)을 확실히 이긴다. 또 역질의(페이스 제안)가 성립하도록 "느리게 뛰면 예측 심박 하락" 단조성 위반율을 0.0%로 맞췄다.

**Q6. 워치 없이 어떻게 검증했나?**
A. 입력을 RunSource 인터페이스로 추상화해 시뮬레이터/Mock(심박/속도 범위 지정 합성)으로 교체 가능하게 했다(QA5). 전체 파이프라인이 단위 테스트(40여 건)로 실행되고, 실제 export된 예측 모델의 추론 일치와 규칙 판정의 무모순성까지 테스트로 재현한다.

**Q7. 이 시스템에서 "AI"는 정확히 어디이고, 왜 도구가 다른가?**
A. "AI = 신경망"이 아니라 **문제별로 맞는 도구를 골랐다**(adr-016). ① **심박 예측 = 신경망**(연속 다수 데이터 + 비선형 + 문헌), ② **개인 Zone2 범위 = 온라인 베이지안**(소수 온라인 라벨 + 콜드스타트 + 불확실성), ③ **판정 = 규칙**(경계 대비 비교는 결정론), ④ **코칭 표현 = 온디바이스 LLM**(생성형). "왜 여기엔 NN을 안 썼나"에 답할 수 있는 것이 곧 설계 근거다.

---

## 12. 더 공부하고 싶을 때 (검색 키워드)

- 신경망 기초: "neural network forward pass", "backpropagation intuition", 3Blue1Brown Neural Networks(유튜브, 시각화 최고)
- 시계열 예측: "time series forecasting", "heart rate prediction LSTM/RNN", Zhu et al. 2025, athlete_hr_predict(LSTM)
- 베이지안: "bayesian updating example", "conjugate prior gaussian", "online learning"
- 러닝 과학: "heart rate zone 2 training", "%HRmax LT1 aerobic threshold", "cardiac drift decoupling", "talk test"
- 안드로이드 AI: "on-device ML", "ML Kit GenAI Prompt API", "TFLite vs custom inference"
- 문서 지도(이 repo 안): 도구 선택 정본 `arch/adr-016`(문제별 NN/Bayesian/규칙) → 쉬운 개념 요약 `ml/AI_EXPLAINED.md` → 실험 기록 `ml/EXPERIMENT_LOG.md` → 설계 근거 `arch/adr-013`(판정=규칙, 역할 재분리), `adr-004`(왜 Bayesian), `adr-002`(LLM 안전) → 명세 `spec/spec-014`(심박 예측 NN), `spec-011`(폰 앱) → 코드 `app/.../pipeline/`의 `HrDynamics.kt`(예측 NN) / `ZoneJudge.kt`(규칙 판정) / `Personalization.kt`(베이지안) → 전체 지도 `arch/architecture-overview.md`

> 폐기 주의: 예전 "신경망 판정기"(`ml/train_mlp.py`, `Zone2Classifier.kt`, adr-005, spec-006)와 "역치 추정 NN"(`ThresholdEstimator`, adr-014, spec-015)은 각각 라벨 순환 결함, Bayesian 중복으로 **폐기/강등**됐다. 코드/문서는 결함 재현 테스트용으로만 남아 있으니(LEGACY), 지금 동작 경로와 혼동하지 말 것.
