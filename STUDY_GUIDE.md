# Zone2 Runner 공부 가이드 — 이 프로젝트, 바닥부터 이해하기

> **누구를 위한 문서?** 이 프로젝트의 기술적인 부분(특히 AI)을 잘 모르는 상태에서, 하나씩 공부하며
> "내가 만든 것을 내가 설명할 수 있는" 수준까지 가기 위한 개인 학습서.
> 사전지식 없다고 가정하고 씀. 수식은 최소, 비유는 최대.
>
> **같이 볼 문서**: `ml/AI_EXPLAINED.md`(AI 개념 요약판). 이 가이드는 그보다 넓고(앱 전체) 깊게(코드까지) 간다.

---

## 0. 공부 순서 (추천 커리큘럼)

하루에 한 챕터씩 봐도 되고, 한 번에 쭉 읽어도 된다. **순서는 지키는 걸 추천** — 뒤로 갈수록 앞 개념을 쓴다.

| 단계 | 챕터 | 내용 | 예상 시간 |
|:---:|------|------|:---:|
| 1 | §1~2 | 큰 그림 + 러닝/심박 기초 용어 | 30분 |
| 2 | §3 | 데이터의 여행(1초 동안 일어나는 일) | 30분 |
| 3 | §4 | **AI ① 신경망(MLP) 판정기** — 제일 중요 | 2시간 |
| 4 | §5 | AI ② 베이지안 개인화 | 1시간 |
| 5 | §6 | AI ③ LLM 코칭 | 30분 |
| 6 | §7 | 시뮬레이터(학습 데이터 공장) | 1시간 |
| 7 | §8 | 앱 구조(화면/저장/센서 교체) | 1시간 |
| 8 | §9 | 직접 해보기(실습) | 2시간+ |
| 9 | §10~12 | 용어 사전 + 예상 질문 Q&A | 복습용 |

---

## 1. 30초 큰 그림

이 앱이 하는 일을 한 문장으로:

> **"달리는 사람의 심박수를 보고, 지금 '지방 연소에 최적인 강도(Zone 2)'인지 AI가 판단해서, 말로 코칭해 준다."**

전체 시스템은 3개의 AI 부품 + 배관(파이프라인)으로 되어 있다:

```
워치(또는 GPS/가짜입력)          폰
┌──────────────┐    ┌─────────────────────────────────────────┐
│ 심박, 위치 수집 │──▶│ ①청소 ②특징만들기 ③MLP판정 ④개인화 ⑤코칭 │──▶ 화면 + 음성
└──────────────┘    └─────────────────────────────────────────┘
```

| AI 부품 | 역할 | 종류 | 코드 |
|------|------|------|------|
| **MLP 판정기** | "지금 Zone 2보다 낮아? 맞아? 높아?" 3지선다 | **진짜 신경망**(학습함) | `ml/train_mlp.py` → `app/.../Zone2Classifier.kt` |
| **베이지안 개인화** | "이 사람의 Zone 2 경계는 몇 bpm?"을 데이터 쌓일수록 정교하게 | 확률 계산(신경망 아님) | `ml/personalization.py` → `app/.../Personalization.kt` |
| **LLM 코칭** | 판정 결과를 자연스러운 한국어 문장으로 | 생성형 AI(폰 안에서 실행) | `app/.../LlmCoach.kt` |

**왜 3개나?** 각자 잘하는 게 다르다. 판정은 빠르고 정확해야 하고(작은 신경망), 개인화는 데이터가 적어도 되는 방법이어야 하고(베이지안), 말은 자연스러워야 한다(LLM). 하나로 다 하려면 각각의 약점이 다 드러난다.

---

## 2. 기초 개념: 러닝과 심박 (이걸 모르면 뒤가 안 읽힘)

### 심박수(HR, Heart Rate)
1분에 심장이 뛰는 횟수. 단위 bpm. 쉴 때 50~70, 전력질주 때 180~200.

### Zone(존)이란
운동 강도를 심박 기준으로 5단계로 나눈 것. **Zone 2 = "옆사람과 대화 가능한 편안한 강도"**. 이 강도가 지방을 태우고 지구력 기초를 만드는 데 제일 효율적이라 러너들이 집착하는 구간이다.

### HRR(여유 심박수) 공식 — 우리 앱의 "출발점"
```
HRR = 최대심박 - 안정심박          (예: 185 - 60 = 125)
Zone2 하한 = 안정심박 + 0.60 x HRR  (예: 60 + 75 = 135 bpm)
Zone2 상한 = 안정심박 + 0.70 x HRR  (예: 60 + 87.5 = 147 bpm)
```
- 최대심박은 실측이 어려워 나이 공식(Tanaka: `208 - 0.7 x 나이`)으로 추정.
- **문제**: 이 공식은 "평균적인 사람" 기준. 개인은 이 경계에서 위아래로 벗어나 있다. → 그래서 **개인화**(§5)가 필요.

### 페이스(pace)와 케이던스(SPM)
- 페이스: 1km를 몇 분에 뛰나. min/km. 낮을수록 빠름. 속도(km/h)와 역수 관계: `페이스 = 60 / 속도`.
- 케이던스(SPM): 1분당 발걸음 수. 보통 160~180.

### 심박 지연(HR lag)과 심혈관 드리프트(Cardiac Drift) — **신경망이 필요한 진짜 이유**
- **지연**: 오르막을 만나 힘들어져도 심박은 20~40초 늦게 올라간다. 심박"만" 보면 판단이 늦는다.
- **드리프트**: 같은 속도로 계속 뛰어도 시간이 지나면(체온 상승, 탈수) 심박이 슬금슬금 오른다. 심박이 올랐다고 "강도가 높아진 것"이 아닐 수 있다.
- 결론: **"심박 147 넘으면 초과!" 같은 단순 규칙은 오르막/장거리에서 오판**한다. 심박+페이스+경사+드리프트를 **동시에** 봐야 하고, 그 복잡한 조합 판단을 신경망에 맡긴 것.

### 디커플링(decoupling)
"심박/페이스 비율"이 러닝 초반 대비 얼마나 커졌나(%). 드리프트를 숫자로 잡는 지표. 초반과 같은 페이스인데 심박만 5% 올랐으면 디커플링 5%.

---

## 3. 데이터의 여행 — 1초 동안 일어나는 일

앱이 켜져 있는 동안 **매초** 아래가 반복된다. 이 흐름이 곧 `RunEngine.kt`(오케스트레이터)다.

```
[1] 샘플 도착: Sample(시각, 심박, 페이스, 케이던스, 경사, 위도, 경도)
     ← 어디서? 셋 중 하나(§8): 워치 / 시뮬레이터 / 가짜입력(Mock)
[2] 청소(OutlierGuard): 심박이 40~220 밖이면 "고장난 값"으로 버리고 직전 값 유지
[3] 특징 만들기(FeatureExtractor): 최근 30~60초 기록으로 숫자 7개 계산 (§4-3)
[4] 판정(Zone2Classifier): 숫자 7개 → 신경망 → "미달 / 유지 / 초과" 중 하나
     (모델이 없으면? 규칙으로 폴백 — 앱은 절대 안 죽음)
[5] 개인화 관측 수집(Personalization): 판정과 별개로, "이 사람의 진짜 경계" 단서를 모아
     5분마다 경계 추정치를 갱신 (§5)
[6] 코칭(Coach): 판정이 바뀌었고 20초 지났으면 → 방향 결정(규칙) → 문장 생성(LLM) → TTS 음성
[7] 기록: 경로 점, 심박/페이스 시계열, 존 체류 시간을 누적 → 종료 시 리포트/저장
```

> 코드로 따라가기: `app/app/src/main/java/com/zone2runner/app/pipeline/RunEngine.kt`의 `onSample()` 함수가 위 [2]~[7]을 순서대로 한다. 50줄 정도니 직접 읽어보자. 이 파일 하나가 이 프로젝트의 "척추"다.

---

## 4. AI ① — MLP 판정기 (신경망, 제일 중요한 챕터)

### 4-1. 신경망이 뭔가: "곱하고, 더하고, 꺾는다"

신경망의 최소 단위(뉴런)가 하는 일은 딱 세 가지다:

```
출력 = 꺾기( 입력1 x 가중치1 + 입력2 x 가중치2 + ... + 보정값 )
```

1. 입력마다 **가중치(weight)를 곱하고**
2. 전부 **더하고**(+보정값 bias)
3. 결과가 음수면 0으로 **꺾는다**(ReLU라고 부름. "음수는 버려"가 전부)

이게 전부다. "학습"이란 **가중치 숫자들을 정답에 맞게 조금씩 조정하는 과정**일 뿐이다.

뉴런 하나는 직선 하나밖에 못 긋지만, 수십 개를 층층이 쌓으면 구불구불한 경계(비선형)를 그릴 수 있다. 우리가 필요한 게 바로 그것 — "오르막이면서 심박이 급상승 중이면서 디커플링이 낮으면 → 아직 초과 아님" 같은 **조건들의 복잡한 조합**.

### 4-2. 우리 신경망의 크기

```
입력 7개 → [1층: 뉴런 32개] → [2층: 뉴런 16개] → 출력 3개(미달/유지/초과 점수)
```
- 가중치 개수: 7x32 + 32x16 + 16x3 + 보정값 = **약 800개의 숫자**. 이게 모델의 전부다.
- ChatGPT류가 수천억 개인 것과 비교하면 초소형. 그래서 폰에서 1ms도 안 걸리고, 별도 AI 엔진 없이 Kotlin 반복문으로 돌린다(§4-6, adr-011).

### 4-3. 입력 7가지 — 신경망에게 주는 "단서들"

신경망은 원시 심박을 그대로 받지 않는다. **사람이 설계한 의미 있는 숫자 7개**(특징, feature)로 가공해 준다. `FEATURE_NAMES` 순서 그대로:

| # | 이름 | 뜻 | 왜 주나 |
|:-:|------|------|------|
| 1 | `hr_norm_u` | 최근 1분 평균 심박이 **개인 상한보다 얼마나 위/아래**인가 (HRR 비율) | 핵심 신호. 0보다 크면 "상한 초과 중" |
| 2 | `hr_norm_l` | 같은 것을 **하한** 기준으로 | 0보다 작으면 "하한 미달 중" |
| 3 | `dHR` | 최근 30초간 심박 상승 속도(bpm/초) | 심박 지연 보정 — "지금은 낮지만 급상승 중"을 미리 감지 |
| 4 | `pace` | 최근 1분 평균 페이스 | 심박과 페이스가 안 맞으면 뭔가 이상함 |
| 5 | `spm` | 케이던스 | 뛰는지 걷는지, 리듬 정보 |
| 6 | `decoupling` | 드리프트 지표(§2) | "심박 상승 = 강도 상승"이 아닐 때를 구분 |
| 7 | `slope` | 경사(%) | 오르막의 심박 상승을 "강도 초과"로 오판하지 않게 |

**포인트**: 1, 2번이 "개인 경계 대비"라는 것. 개인화(§5)가 경계를 갱신하면 신경망 입력 자체가 개인 기준으로 정규화된다. **개인화와 신경망이 결합되는 지점이 바로 여기다.**

### 4-4. 학습 데이터: 정답(라벨)은 어디서 났나 — 이 프로젝트의 가장 영리한 부분

신경망을 가르치려면 "이 상황은 미달/유지/초과"라는 **정답표**가 필요하다. 문제: 진짜 사람의 Zone 2 경계는 실험실 검사 없이는 아무도 모른다. 정답이 없는데 어떻게 학습하나?

**답: 가상 러너를 만들었다** (`ml/simulator.py`).

1. 가상 러너 150명 생성. 각자에게 나이, 안정심박, 그리고 **진짜 개인 경계**(공식 70% 근처에서 개인차만큼 벗어난 값)를 부여한다. **시뮬레이터 세상에서는 우리가 조물주라서 정답을 안다.**
2. 각 러너가 30분씩 8세션을 "뛴다". 물리 법칙(심박 지연, 드리프트, 노이즈, §7)에 따라 심박 곡선이 만들어진다.
3. 매 순간의 라벨 = "지속 강도가 그 러너의 **진짜** 경계보다 아래/안/위인가"로 기계적으로 계산.
4. 결과: 특징 7개 + 정답 라벨이 붙은 **약 40만 개의 학습 샘플**.

이러면 "정답 없음" 문제가 풀린다. 대신 **"시뮬레이터가 현실과 얼마나 닮았나"가 새 리스크**가 되는데, 이건 물리 모델(지연/드리프트)을 문헌 기반으로 넣고 노이즈를 섞는 것으로 방어했다. 발표 때 "라벨은 시뮬레이터에서 얻었다"는 질문 대비 필수(§11 Q5).

### 4-5. 학습 과정: 신경망을 가르치는 법 (`ml/train_mlp.py`)

```
반복 (최대 300바퀴):
  1. 학습 데이터에서 512개씩 꺼낸다
  2. 현재 가중치로 예측해 본다 (순전파)
  3. 정답과 비교해 "얼마나 틀렸나" 점수를 낸다 (손실, CrossEntropy)
  4. 각 가중치를 "어느 쪽으로 조금 움직이면 덜 틀릴까" 계산해 (역전파, 미분)
     그 방향으로 아주 조금(학습률 0.001) 움직인다 (Adam 옵티마이저)
  5. 검증 데이터(학습에 안 쓴 러너들) 성적이 15바퀴 동안 안 오르면 중단 (early stopping)
```

우리가 쓴 안전장치 두 개는 시험 공부 비유로 이해하면 쉽다:
- **러너 단위로 train/검증/시험 분리**: 같은 러너의 데이터가 학습과 시험에 둘 다 들어가면 "그 사람을 외운" 컨닝이 된다. 러너 자체를 갈랐다(모의고사 문제와 수능 문제를 다른 출제자가 냄).
- **노이즈 증강**: 학습 때 입력에 일부러 작은 잡음을 섞는다. 문제를 조금씩 바꿔 풀게 해서 "암기"가 아닌 "이해"를 시키는 것. 강건성(QA2)에 기여.

**결과 성적표** (`ml/EXPERIMENT_LOG.md`): 규칙만 0.544 → MLP 0.802 → MLP+개인화 **0.826**. 그리고 실사용 관점 지표인 **"코칭 방향을 정반대로 말한 비율" 0.4%**(QA1 0.996), 이상치 기각 100%(QA2).

### 4-6. 학습은 파이썬인데 폰은 Kotlin — 모델을 어떻게 옮겼나

여기가 재밌는 부분. 흐름:

```
[PyTorch 학습] → 성능 확인 (canonical, 논문/실험용)
[sklearn 재적합] → 같은 구조(7-32-16-3)로 다시 학습 (export_model.py)
[JSON 추출] → 가중치 800개 + 정규화 상수를 텍스트 파일로 (zone2_mlp.json, 18KB)
[폰에서 로드] → Kotlin이 JSON 읽어서 곱하기/더하기 직접 수행 (Zone2Classifier.kt)
```

**왜 TFLite(구글의 모바일 AI 엔진)를 안 썼나?** (adr-011) 모델이 겨우 800개 숫자라서. 몇 MB짜리 엔진을 붙이는 건 "종이비행기 접으려고 공작기계 사는 격". 대신 추론 코드를 직접 짰다 — 그래서 **AI가 어떻게 계산하는지 코드에 그대로 보인다**. 직접 보자:

```kotlin
// app/.../pipeline/Zone2Classifier.kt — classify() 핵심부 (실제 코드)
var x = DoubleArray(feat.size) { (feat[it] - mean[it]) / scale[it] }  // ① 표준화
for ((idx, layer) in layers.withIndex()) {
    val out = DoubleArray(layer.b.size)
    for (o in out.indices) {                 // ② 각 뉴런마다:
        var s = layer.b[o]                   //    보정값에서 시작
        val row = layer.w[o]
        for (i in x.indices) s += row[i] * x[i]  //    곱하고 더한다
        out[o] = s
    }
    if (idx < layers.size - 1)               // ③ 마지막 층 빼고
        for (o in out.indices) if (out[o] < 0) out[o] = 0.0  //    음수는 0으로(ReLU)
    x = out
}
// ④ 마지막 x = 점수 3개 [미달점수, 유지점수, 초과점수] → 제일 큰 것이 판정
```

- ① 표준화: 특징마다 크기가 다르니(페이스는 6쯤, spm은 170쯤) "평균 빼고 표준편차로 나눠" 비슷한 크기로 맞춘다. 학습 때와 **같은** 상수를 써야 해서 JSON에 함께 저장돼 있다.
- ②~③: §4-1에서 말한 "곱하고 더하고 꺾기" 그대로. 신경망의 실체가 이중 for문이라는 것.
- ④ softmax: 점수 3개를 "확률처럼 합이 1"이 되게 바꾸는 마무리(코드의 `softmax()`). 판정 자체는 그냥 최대값 고르기(argmax).

**검증**: 이 Kotlin 코드가 파이썬 학습 모델과 같은 답을 내는지 단위 테스트로 확인했다(`Zone2ClassifierTest.kt` — 실제 JSON을 로드해 "높은 심박 특징 → 초과, 낮은 특징 → 미달" + 성적표 재현).

---

## 5. AI ② — 베이지안 개인화 (신경망 아님, 그런데 왜 이게 AI인가)

### 5-1. 푸는 문제

공식 Zone 2 상한이 147인데 진짜는 154인 사람이 있다. 공식대로면 이 사람에게 "느리게!"를 7 bpm이나 일찍 외친다. **개인의 진짜 경계를 어떻게 알아낼까? 그것도 실험실 없이, 몇 번의 러닝 데이터만으로?**

### 5-2. 베이지안 사고방식 — 몸무게 재기 비유

집에 부정확한 체중계가 있다 치자.
- **사전(prior)**: "나는 대충 70kg일 것" (지난달 병원 기록) — 확신도 중간
- **관측(evidence)**: 체중계가 73kg을 가리킴 — 근데 이 체중계는 오차가 큼
- **사후(posterior)**: "그럼 71kg쯤이겠네" — **둘 사이, 그런데 더 믿을 만한 쪽에 가깝게**

핵심 규칙 두 개:
1. 새 정보가 올 때마다 믿음을 **"확신도에 비례해 가중평균"으로 갱신**한다.
2. 갱신할수록 **불확실성(σ)이 줄어든다** — 정보가 쌓이니 당연.

### 5-3. 우리 코드에 그대로 있다 (`Personalization.kt`, 15줄)

```kotlin
// 사전: 공식 상한(예: 147 bpm), 불확실성 σ=8
// 관측: 러닝 중 "디커플링이 임계 부근일 때의 지속 심박" (경계 근처에 있었다는 단서)
fun update(zBpm: Double, obsSd: Double = 10.0) {
    val p0 = 1.0 / variance          // 내 기존 믿음의 확신도 (분산의 역수 = 정밀도)
    val p1 = 1.0 / (obsSd * obsSd)   // 관측의 확신도
    muUpper = (muUpper * p0 + z * p1) / (p0 + p1)  // ← 확신도 가중평균. 이 한 줄이 베이지안 전부
    variance = 1.0 / (p0 + p1)       // 불확실성은 반드시 감소
    muUpper = muUpper.coerceIn(mu0 - 15, mu0 + 15) // 안전 가드: 공식에서 ±15 bpm 이상 못 벗어남
}
```

숫자로 따라가 보자. 사전 147(σ=8), 관측 156(σ=10):
```
p0 = 1/64 = 0.0156,  p1 = 1/100 = 0.01
새 추정 = (147x0.0156 + 156x0.01) / 0.0256 ≈ 150.5
새 σ = √(1/0.0256) ≈ 6.25   ← 8에서 줄었다
```
관측이 쌓일수록 147 → 150.5 → 152.8 → ... 진짜 경계(154)로 **수렴**하고 σ는 계속 준다. 이게 QA3(적응성)의 메커니즘이고, ml 실험에서 "세션 누적 시 오차 5→2bpm" 으로 실증했다.

### 5-4. 왜 여기엔 신경망을 안 썼나 (설계 판단, adr-004)

- 개인별 데이터가 **몇 세션밖에 없다** → 신경망은 데이터 부족으로 과적합.
- 시작부터 동작해야 한다 → 베이지안은 사전(공식)이 있어 **데이터 0개여도 합리적**.
- 불확실성을 스스로 안다(σ) → "아직 자신 없음"을 표현 가능. 신경망은 못 함.
- **"AI = 신경망"이 아니다.** 데이터로 스스로 개선되는 시스템이면 AI다. 이건 발표 때 어필 포인트.

### 5-5. 정직한 한계

관측(디커플링 임계 부근 심박 추출)이 편향되면 엉뚱한 곳으로 수렴한다. 실제 실험에서 임계추출(Conconi 방식)의 편향 문제를 확인했고, **"관측이 좋으면 수렴한다(메커니즘 검증), 좋은 관측 만들기는 향후 과제"**로 문서화했다(HANDOFF §5). 한계를 아는 것도 설계 능력.

---

## 6. AI ③ — LLM 코칭 (Gemini Nano)

### 6-1. LLM이 뭔가, 그리고 "온디바이스"가 왜 중요한가

LLM = 문장을 만들어 내는 생성형 AI(ChatGPT류). 우리는 **Gemini Nano** — 구글이 폰 안에 넣어둔 소형 LLM — 를 쓴다. 인터넷 없이 폰 칩으로만 동작(온디바이스). 러닝은 밖에서 하니까 오프라인 동작이 필수 + 심박은 민감정보라 폰 밖으로 안 내보내는 게 안전. 실기기(S26)에서 동작 확인 완료(adr-007, `llm-verify/`).

### 6-2. 설계의 핵심: "방향은 규칙이, 말투만 LLM이" (adr-002)

LLM의 약점 = 가끔 헛소리(환각). 운동 코칭에서 "빨리 뛰세요"를 반대로 말하면 위험하다. 그래서:

```
판정(초과) → 규칙이 방향 확정: "느리게 하라고 말할 것"   ← AI가 못 바꾸는 부분
           → LLM에게: "이 방향으로, 오르막 상황 반영해서, 25자 내외 한 문장만" (방향 잠금 프롬프트)
           → 출력 가드: 길이 제한, 따옴표 제거, 첫 문장만   ← 이상한 출력 차단
           → 실패하면? 미리 써둔 규칙 문장으로 폴백. 코칭이 끊기는 일은 없음
```

`LlmCoach.kt`의 `buildPrompt()`와 `guard()`를 열어보면 위가 코드 그대로 있다. **"생성형 AI를 안전하게 쓰는 표준 패턴"**(방향 잠금 + 출력 가드 + 폴백)이라 다른 프로젝트에도 써먹을 수 있는 지식.

---

## 7. 시뮬레이터 — 학습 데이터 공장 (`ml/simulator.py`, `app/.../RunSimulator.kt`)

가상 러너가 "물리적으로 그럴듯하게" 뛰게 만드는 부품. 신경망 학습 데이터(§4-4)가 여기서 나오고, 앱의 시뮬레이션 모드도 이것의 Kotlin 포팅이다. 넣은 물리 3가지:

1. **심박 지연**: 목표 강도로 심박이 **서서히** 따라감. `hr[i] = hr[i-1] + (목표 - hr[i-1]) / τ` — τ(타우, 20~40초)가 클수록 느린 심장. "지수 이동"이라는 고전 물리 패턴.
2. **드리프트**: 경계보다 높은 강도로 뛰는 동안 심박이 추가로 슬금슬금 상승(누적 피로 모사).
3. **노이즈**: 광학 센서 오차를 가우시안 잡음으로.

여기에 지형(오르막/내리막 구간)과 강도 변화(인터벌)를 랜덤 배치. **러너마다 파라미터(경계, τ, 드리프트 정도, 노이즈)가 달라서** 신경망이 "다양한 사람"을 경험하게 된다.

> 재미: 앱의 "가짜 라이브(Mock)" 모드(§8)는 이것보다 단순한 실시간 버전 — 심박/속도가 지정 범위 안에서 랜덤워크하고 GPS가 실제로 이동한다.

---

## 8. 앱 구조 — AI를 담는 그릇

### 8-1. 화면 6개 (폰, `app/`)

```
Home ──러닝 시작──▶ Run(라이브: 지도+심박+판정+코칭) ──종료──▶ Report(차트+분석+경로)
 ├─ History(기록 목록 → 리포트 다시 보기)
 ├─ Profile(나이/안정심박 → Zone2 목표 계산)
 └─ MockConfig(가짜 라이브 설정: 심박/속도 범위)
```

### 8-2. 이 프로젝트에서 제일 자랑할 만한 앱 설계: RunSource 추상화

파이프라인(§3)은 "1초에 하나씩 Sample을 받는다"는 것만 안다. **누가 주는지 모른다.** 주는 쪽은 3종:

| 소스 | 무엇 | 언제 쓰나 |
|------|------|------|
| `SimulatedRunSource` | 시뮬 세션 70배속 재생 | 데모, 파이프라인 점검 |
| `LiveRunSource` | 진짜 GPS + 워치 심박(블루투스 Data Layer) | 실제 러닝 |
| `MockRunSource` | 심박/속도 범위 지정 실시간 합성 | 워치 없이 개발/QA/시연 |

이렇게 만들면: 워치가 없어도 전체를 검증할 수 있다(= QA5 "테스트 가능성" 달성). 나중에 새 센서가 나와도 소스 하나만 추가하면 된다. **"인터페이스에 의존하고 구현에 의존하지 마라"** 라는 소프트웨어 설계 격언의 실물 예제.

### 8-3. 저장

- 세션: 러닝 1회 = JSON 파일 1개(`filesDir/sessions/`). DB 없이. 규모가 작을 땐 단순한 게 정의.
- 프로필/Mock 설정: SharedPreferences(안드로이드 기본 키-값 저장소).

### 8-4. 워치(`wear/`)

수집+표시 전담(판정은 폰 — adr-001의 역할 분담). 심박을 측정해 화면에 존 게이지로 보여주면서, 동시에 폰으로 `/hr` 메시지 전송(`HrForwarder`). 워치의 존 표시는 %HRmax 5구간 경량판 — 정밀 판정은 폰 MLP가 한다는 이중 구조.

---

## 9. 직접 해보기 (실습 — 이걸 해야 진짜 내 것이 됨)

**준비**: `app/` 폴더에서 실행. JAVA_HOME은 Android Studio JBR.

1. **테스트 돌려보기** (5분): `./gradlew.bat testDebugUnitTest` → 13건 통과 확인. 그다음 `app/app/src/test/.../Zone2ClassifierTest.kt`를 열어 "실제 모델 JSON을 읽어 추론"하는 테스트를 읽는다.
2. **신경망 손계산 체험** (15분): `Zone2ClassifierTest.kt`의 `forwardPass_singleLayer_argmaxIsCorrect`에 있는 초미니 모델(입력 2, 출력 3)을 종이에 그려 §4-6 코드를 손으로 따라 계산해 본다. "신경망 = 곱셈과 덧셈"을 체감하는 게 목적.
3. **모델 성적 재현** (10분): `python ml/export_model.py` 실행 → 규칙 0.544 / MLP 0.826 / QA1 / QA2가 출력되는 걸 직접 본다. (`.venv` 필요: HANDOFF §5 참고)
4. **모델 구조 바꿔보기** (30분): `export_model.py`의 `hidden_layer_sizes=(32, 16)`을 `(8,)`로 줄여 재실행 → 정확도가 어떻게 변하나? `(64, 32)`로 늘리면? "모델 크기 vs 성능" 감각이 생긴다.
5. **베이지안 손계산** (15분): §5-3의 숫자 예제를 계산기로 두 번 반복해 보고, `PipelineTest.kt`의 `personalization_movesTowardObservationAndStaysBounded`와 비교.
6. **Mock 모드로 판정 검증** (폰 있으면, 20분): 앱 설치 → 가짜 라이브 → 심박 범위를 일부러 "초과" 수준(160~180)으로 → 앱이 "느리게" 코칭하는지, 리포트 존 분포가 주황(초과) 위주인지 확인. **이게 QA1을 눈으로 검증하는 것.**

---

## 10. 용어 미니 사전 (모르는 말 나오면 여기)

| 용어 | 한 줄 설명 |
|------|------|
| MLP | 가장 기본형 신경망. 층층이 "곱하고 더하고 꺾기" |
| 특징(feature) | 원시 데이터를 모델이 먹기 좋게 가공한 숫자 |
| 라벨(label) | 학습용 정답 |
| 순전파 | 입력→출력 방향으로 계산하는 것(=추론) |
| 역전파 | 틀린 만큼 가중치를 거꾸로 조정하는 학습 알고리즘 |
| 손실(loss) | "얼마나 틀렸나" 점수. 학습은 이걸 낮추는 게임 |
| 에폭(epoch) | 학습 데이터 전체를 1바퀴 도는 단위 |
| 과적합 | 문제를 암기해서 새 문제를 못 푸는 상태 |
| early stopping | 검증 성적이 안 오르면 학습 중단(과적합 방지) |
| ReLU | 음수를 0으로 꺾는 함수 |
| softmax | 점수들을 합이 1인 확률처럼 변환 |
| StandardScaler | 평균 0, 표준편차 1로 입력 크기 통일 |
| prior/posterior | (베이지안) 갱신 전 믿음 / 갱신 후 믿음 |
| σ(시그마) | 불확실성의 크기. 작을수록 확신 |
| 온디바이스 | 서버 없이 기기 안에서 AI 실행 |
| 폴백(fallback) | 주 방법 실패 시 예비 방법으로 전환 |
| TTS | 글자 → 음성 변환 |
| Data Layer | 워치↔폰 블루투스 메시지 통로(구글 API) |
| QA1~QA5 | 우리 품질 목표(spec-002): 정확성/강건성/적응성/효율성/테스트가능성 |

---

## 11. 예상 질문 Q&A (발표/심사 대비 — 소리 내어 답해보기)

**Q1. 왜 신경망(MLP)을 썼나? 규칙으로 충분하지 않나?**
A. 단일 심박 임계값은 오르막(심박 지연), 장거리(드리프트), 노이즈 상황에서 방향을 오판한다. 실측: 규칙 0.544 vs MLP 0.826. 심박/페이스/경사/드리프트의 **비선형 조합** 판단이 필요했고, 그게 신경망이 잘하는 일이다. 단, 규칙을 버린 게 아니라 가드/폴백으로 밑에 깔았다(하이브리드).

**Q2. 개인화에 왜 딥러닝을 안 썼나?**
A. 개인별 데이터가 세션 몇 개뿐이라 신경망은 과적합한다. 베이지안은 공식을 사전분포로 써서 **데이터 0개부터 동작**하고, 쌓일수록 개인 경계로 수렴하며, 불확실성(σ)을 스스로 표현한다. 문제 조건(콜드스타트, 소량 데이터)에 맞는 도구 선택이 곧 설계다.

**Q3. LLM이 잘못된 코칭을 하면 위험하지 않나?**
A. 방향(가속/감속)은 규칙이 확정하고 LLM은 표현만 한다(방향 잠금 프롬프트). 출력 가드로 길이/형식을 강제하고, 실패 시 정적 문장 폴백. LLM이 안전에 개입할 수 없는 구조다.

**Q4. 학습 라벨(정답)은 어디서 얻었나?**
A. 실측 정답(개인 경계)은 랩 검사 없이는 불가하므로, 물리 기반 시뮬레이터로 가상 러너 150명(정답 경계를 알고 있는)을 만들어 40만 샘플을 생성했다. 시뮬-현실 간극은 심박 지연/드리프트/노이즈의 물리 모델링과 노이즈 증강으로 완화했고, 한계로도 명시했다.

**Q5. 정확도 82.6%면 낮은 것 아닌가?**
A. 3분류 샘플 단위 정확도이고, 경계 "근처" 샘플의 혼동이 대부분이다. 실사용 관점 지표는 "코칭 방향을 정반대로 낸 비율"인데 이것이 0.4%(QA1 0.996)로 목표(95%)를 초과 달성. 지표를 사용자 경험 기준으로 정의한 것 자체가 설계 판단이다.

**Q6. 워치 없이 어떻게 검증했나?**
A. 입력을 RunSource 인터페이스로 추상화해 시뮬레이터/Mock(심박·속도 범위 지정 합성)으로 교체 가능하게 했다(QA5). 전체 파이프라인이 단위 테스트 13건으로 실행되고, 실제 export된 모델의 추론 일치까지 테스트로 재현한다.

**Q7. 이 시스템에서 "AI"는 정확히 어디인가?**
A. 3곳. ① 학습된 MLP 판정기(교육과정의 NN 요건), ② 베이지안 적응 추정(학습형 확률 모델), ③ 온디바이스 LLM(생성형). 각각 판정 정확성, 개인화, 자연어 표현이라는 다른 문제를 푼다.

---

## 12. 더 공부하고 싶을 때 (검색 키워드)

- 신경망 기초: "neural network forward pass", "backpropagation intuition", 3Blue1Brown Neural Networks(유튜브, 시각화 최고)
- 베이지안: "bayesian updating example", "conjugate prior gaussian"
- 러닝 과학: "heart rate zone 2 training", "cardiac drift decoupling"
- 안드로이드 AI: "on-device ML", "ML Kit GenAI Prompt API", "TFLite vs custom inference"
- 문서 지도(이 repo 안): 개념 요약 `ml/AI_EXPLAINED.md` → 실험 기록 `ml/EXPERIMENT_LOG.md` → 설계 근거 `arch/adr-005`(왜 NN), `adr-004`(왜 Bayesian), `adr-002`(LLM 안전) → 명세 `spec/spec-006`(MLP), `spec-011`(폰 앱) → 전체 지도 `arch/architecture-overview.md`
