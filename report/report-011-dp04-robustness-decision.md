# Report-011: DP4 (강건성) — 인증 보고서 "03. 설계 / Architectural Decision" 원고

- **날짜**: 2026-07-31 (v1)
- **용도**: 인증 보고서 PPT 원고. 인증과제 샘플(씬스틸러) DP 형식 3장 + Appendix 1장.
- **작성 원칙**: 전문가가 아니어도 읽히는 쉬운 한국어. 압축 표기와 영문 용어 없이 설명하는 문장으로. 영문 용어는 Appendix 용어표에만.
- **정본**: `arch/dp/dp-04-robustness/dp-04-robustness-decision.md` — 이 문서는 그 표현물.

---

## 슬라이드 1 — DP4. 설계 문제 정의

### 1. 배경

- 이 앱에 들어오는 신호는 깨끗하지 않다. 손목 광학 심박계는 달리는 중 팔 흔들림으로 값이 튀는 것이 알려진 한계이고, GPS도 잡음이 섞이며, 연결이 잠깐 끊기기도 한다.
- 판정 결과는 화면 표시로 끝나지 않고 **"속도를 줄이세요" 같은 행동 지시로 이어진다.** 튄 값 하나가 지시가 되면 사용자는 노이즈에 맞춰 달리게 된다.
- 그래서 **노이즈가 일상인 신호에서 판정과 지시를 지키는 구조**를 고르는 것이 이 설계 결정이다.

### 2. 문제점

- **오판의 비용이 비대칭이다.** 판정이 조금 늦는 것보다, 튄 값으로 잘못된 지시를 내보내는 것이 더 나쁘다.
- **경계 근처에서 판정이 깜빡인다.** 심박이 경계를 오르내리면 순간값 판정은 초 단위로 뒤집혀 신뢰를 잃는다.
- **노이즈 폭이 사람마다 다르다.** 고정된 필터 하나로 모두를 맞출 수 없다.
- **정답지가 없다.** 어느 판정이 맞았는지 채점할 수 없으니, 강건성은 판정의 일관성(번복 억제, 이상값 차단)으로 세워야 한다.

---

## 슬라이드 2 — DP4. 설계안 결정

| | **(1안) 계층 방어 판정 아키텍처** (채택) | **(2안) 즉시 반응 판정 아키텍처** |
|---|---|---|
| **한 줄 소개** | 성격이 다른 방어를 네 층으로 겹쳐, 잘못된 지시가 나가지 않게 하는 구조 | 짧은 이동평균 하나로 다듬고 바로 판정해 지시까지 잇는 구조 |
| **구조** | (그림: `arch/diagrams/02b-component-cnc-simple.png`)<br/>① 있을 수 없는 값(심박 40 미만/220 초과, 낡은 값)은 문에서 버린다. ② 화면 표시는 순간값으로 빠르게 하되 이중 문턱으로 깜빡임을 막고, 코칭은 60초 지속된 상태에만 반응한다. ③ 추세는 창 통계(기울기+신뢰도)로 읽고, 문턱은 그 사람 노이즈의 배수로 자가 보정한다. ④ 센서가 빠지면 다음 소스로 갈아탄다 | (그림: `arch/dp/dp-04-robustness/images/dp-04-robustness-counter-simple.png`)<br/>최근 몇 초의 이동평균으로 신호를 다듬은 뒤, 순간값을 경계와 바로 비교해 판정하고 코칭까지 그대로 잇는다. 구조가 단순하고 반응이 수 초로 빠르다 |
| **장점** | 강건성 ★★★★★ (이상값 차단, 번복 억제, 두절에도 기능 유지)<br/>테스트가능성 ★★★★ (층이 독립 모듈이라 층별로 검증할 수 있다) | 수행효율성 ★★★★★ (수 초 반응, 계산 최소, 구조 단순)<br/>기능정확성 ★★★ (진짜 급변을 놓치지 않는다 — 단 노이즈와 구별 못 함) |
| **단점** | 수행효율성 ★★★★ (코칭 반응이 최대 60초 늦다 — 의도된 비용)<br/>기능정확성 ★★★★ (방어가 과하면 진짜 변화도 늦게 반영 — 최적점 미검증, 두 안 공통) | 강건성 ★★ (깜빡임과 스파이크가 통과한다 — 방어가 평활 하나)<br/>제어가능성 ★★★ (오판이 행동 지시로 직행한다 — 억제 층 없음) |
| **Trade Off** | 오판이 지시가 되는 것을 구조로 막는다. 대신 코칭 반응이 최대 60초 늦다(의도적 선택 — 화면 표시는 즉시) | 변화가 수 초 안에 반영되고 단순하다. 대신 평활이 놓친 노이즈가 그대로 지시가 되고, 경계 근처 번복을 막을 장치가 없다 |

---

## 슬라이드 3 — DP4. 설계결정 및 근거

### 1. 최종 채택 구조

**후보 1: 계층 방어 판정 아키텍처 채택**

### 2. 채택 근거

- **오판의 비용이 비대칭이다.** 코칭은 행동 지시라 잘못된 지시가 늦은 지시보다 비싸다. 표시(빠름)와 코칭(엄격)의 기준을 분리해 이 비대칭에 맞췄다. 2안은 두 출력이 같은 값에 매여 있다.
- **한 층으로는 못 막는다.** 스파이크, 깜빡임, 추세 오독, 두절은 성격이 다른 노이즈다. 평활 창 하나는 이 모두와 싸워야 하고, 어느 쪽으로 조정해도 반대쪽이 뚫린다.
- **사람마다 자가 보정된다.** 문턱이 고정 숫자가 아니라 그 사람 노이즈의 배수라, 노이즈가 크든 작든 같은 오경보율이 유지된다.
- **표준 기법의 조립이다.** 이중 문턱(온도조절기와 같은 원리), 지속 조건(산업 경보 표준), 노이즈 배수 문턱(통계 관리도) — 근거 없는 자체 발명이 없다.
- **검증할 수 있다.** 강건성을 일관성 지표(이상값 기각률, 판정 번복률)로 정의했고, 층별 모듈이라 시뮬로 노이즈를 주입해 층마다 검증한다.

### 3. 보완 계획

- **단기**: 시뮬 측정은 완료했다(검증 파트 QA4 수록 — 이상치 기각율 100%, 번복 73% 억제). 남은 것은 실주행에서의 재측정이다.
- **장기**: 지속 시간(60초), 문턱 폭 같은 상수는 문헌 근거 출발점이다 — 실주행 기각/번복 기록으로 조정한다. 두절이 길 때 판정 보류를 알리는 표시를 다듬는다.

---

## Appendix 슬라이드 — DP4 상세 설계 내역

**문제점이 걸리는 품질 속성**

- 손목 심박의 순간 스파이크 → 강건성(입력 차단)
- 경계 근처 판정 깜빡임 → 강건성(번복 억제)
- 오판이 행동 지시로 직행 → 제어가능성(지시에 더 엄격한 기준)
- 노이즈 폭의 개인차 → 기능적응성(자가 보정 문턱)
- 신호 두절과 센서 부재 → 강건성(기능 유지)
- 정답지가 없어 채점 불가 → 일관성 지표로 대응
- 방어가 과하면 진짜 변화도 늦는다 → 수행효율성(비용의 정직한 인정)

**채택안의 방어 4층** (각 층은 서로 다른 노이즈를 막는다)

| 층 | 하는 일 | 막는 것 |
|---|---|---|
| 1. 입력 가드레일 | 생리 범위 밖 값과 낡은 값을 문에서 버린다 | 순간 스파이크, 낡은 값 |
| 2. 이중 기준 판정 | 표시는 순간값+이중 문턱, 코칭은 60초 지속 조건 | 깜빡임, 순간 이탈의 지시 직행 |
| 3. 통계 평탄화 | 추세는 창 기울기+신뢰도로, 문턱은 개인 노이즈의 배수로 | 잡음의 추세 오독, 노이즈 개인차 |
| 4. 소스 폴백 | 센서 부재/두절 시 다음 소스로 갈아탄다 | 두절, GPS 튐 |

**QA 별점 종합**

| 품질 속성 | (1안) 계층 방어 판정 | (2안) 즉시 반응 판정 |
|---|:---:|:---:|
| 강건성 | ★★★★★ | ★★ |
| 제어가능성 | ★★★★★ | ★★★ |
| 테스트가능성 | ★★★★ | ★★★ |
| 기능적응성 | ★★★★ | ★★ |
| 수행효율성 | ★★★★ | ★★★★★ |
| 기능정확성 | ★★★★ | ★★★ (잠재, 미검증) |

**평가의 조건**: 이 별점은 손목 광학 심박, 야외 GPS, 출력이 행동 지시, 정답지 없음이라는 이 과제 조건에서의 평가다. 조건이 다르면(가슴 스트랩 심전도급 신호, 출력이 기록/표시 위주) 2안이 우선될 수 있다.

**예상 질문**

- "코칭이 60초 늦으면 이미 늦은 것 아닌가?" → Zone 2 훈련의 목적은 초 단위 반응이 아니라 강도 유지다. 조금 늦은 올바른 지시가 빠른 잘못된 지시보다 낫다. 위험 심박은 별도 안전 규칙이 즉시 감시하고, 화면 표시는 순간값이라 빠르다.
- "이동평균 창을 잘 고르면 2안도 충분하지 않나?" → 창 하나로는 스파이크 억제(긴 창)와 반응성(짧은 창)을 동시에 못 잡고, 경계 근처 번복은 창 길이와 무관하게 남는다. 번복 억제는 평활이 아니라 판정 규칙(이중 문턱, 지속 조건)의 문제다.

**용어 대조(심사용)**: 들어올 때/나갈 때 문턱 분리 = hysteresis / 판정 번복 = flicker, chattering / 일정 시간 지속 후 반응 = on-delay(경보 관리 표준 ISA-18.2) / 개인 노이즈 배수 문턱 = k·σ(통계적 공정 관리) / 두절 시 갈아타기 = graceful degradation

**근거 문헌**: 슈미트 트리거(이중 문턱 표준 기법) / ISA-18.2(경보 지속 조건과 데드밴드) / Shewhart 관리도(산포 배수 관리한계) / 손목 광학 심박의 운동 중 오차(검증 연구들의 일관된 보고)

상세 = `arch/dp/dp-04-robustness/` (decision / research / counter-catalog)
