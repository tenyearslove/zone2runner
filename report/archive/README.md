# report/archive — 탈락/대체된 보고서 (역사 기록)

현재 서사에 **반영되지 않는** 발표/보고용 문서를 모아둔 폴더다. "무엇을 설계했고 왜 접었는가"는 설계 능력의 증거이므로 삭제하지 않고 보존한다. 내용은 이동 시점 그대로다(본문 속 문서 경로는 이동 전 기준일 수 있음).

| 문서 | 다뤘던 것 | 왜 탈락했나 | 대체 |
|---|---|---|---|
| report-006 | DP1 = 가까운 미래 심박 예측 아키텍처 보고 | 예측 참값 부재로 검증 짜고치기 → 예측 드롭(adr-024) | adr-024 (관측 분석 엔진), FR3용 신규 보고(예정) |
| report-005 | gray-box / PINN 학습 노트(ODE 위에 NN 얹기) | 예측 자체를 드롭해 NN 얹을 대상 소멸 | adr-024 |

현행 보고서는 `report/`, 탈락 결정은 `arch/archive/`, 탈락 명세는 `spec/archive/` 참조.
