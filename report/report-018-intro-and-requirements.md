# Report-018: 과제 소개 + 요구사항 분석 — 인증 보고서 "01"/"02" 파트

- **날짜**: 2026-08-04 (QAS 타당성 감사 반영)
- **용도**: 인증 보고서 PPT 원고. 샘플 p3~6 형식(과제 배경/필요성 / 과제 개요 / FR/제약 / QA 선정).
- **정본**: FR/제약 = `spec/spec-001`, QA = `spec/spec-002` — 이 문서는 그 표현물.

---

## 슬라이드 0 — 표지

> **Zone2Runner — 개인화 유산소(Zone 2) 러닝 코칭 앱**
> (AI 기능/품질 요구사항 포함)
>
> 소속: [소속 사업부 / 부서 기입] / 이름: 성시원

## 슬라이드 0-1 — 목차

| 장 | 내용 |
|---|---|
| 01. 과제 소개 | 과제 배경과 필요성 / 과제 개요 |
| 02. 요구사항 분석 | 기능 요구사항 및 제약사항 / 품질 요구사항(QA 선정) |
| 03. 설계 | Architectural Decision(DP1~DP5) / 최종 Architecture |
| 04. 구현 및 검증 | 구현(화면 8종) / 품질속성 검증(실측) |
| 05. 결론 | 주요 성과 / 향후 계획 |
| Appendix | 지표 산출 근거, Use Case, Context, DP 상세, 아키텍처 3뷰, 검증 결과 상세 |

---

## 슬라이드 1 — 과제 소개: 배경과 필요성

### 과제 배경

- **Zone 2 훈련의 확산**: 낮은 강도(대화가 가능한 수준)를 오래 유지하는 유산소 기초 훈련이 일반 러너에게 널리 권장되고 있다. 문제는 그 강도를 유지하기가 어렵다는 점이다 — 초보일수록 자기도 모르게 강도가 올라간다.
- **개인차의 벽**: Zone 2의 기준이 되는 개인 심박 경계는 사람마다 다르고, 최대심박을 주는 나이 공식의 개인 오차가 ±10~15bpm에 이르고, 임계 비율의 개인차까지 겹치면 경계는 그 이상 어긋날 수 있다. 경계가 10bpm 어긋나면 — 존 폭이 프로필에 따라 약 12~18bpm이니 절반을 넘는다 — 사실상 다른 운동을 하게 된다.
- **온디바이스 AI의 성숙**: 폰 안에서 도는 소형 LLM(Gemini Nano)과 워치 센서 생태계가 갖춰져, 서버 없이도 실시간 개인화 코칭을 만들 수 있는 조건이 마련됐다.

### 과제 필요성

- **기존 러닝 앱의 한계**: 대부분 존 표시까지만 한다 — 그 경계가 왜 그 값인지, 판정이 왜 그렇게 났는지 설명하지 않고, 개인의 실제 반응에 맞춰 경계를 조정해 가는 피드백 과정이 없다.
- **AI 코칭의 신뢰 문제**: 생성형 AI가 그럴듯한 말을 지어낼 수 있는 시대에, 운동 강도라는 "행동 지시"를 맡기려면 — 설명이 실제 판단과 일치하고, 어긋난 출력이 차단되고, 실패해도 기능이 이어지는 구조가 필요하다.
- **정리**: 이 과제는 "더 똑똑한 모델"이 아니라 **"믿고 따라도 되는 AI 코칭 시스템의 설계"**를 다룬다.

---

## 슬라이드 2 — 과제 개요

| 항목 | 내용 |
|---|---|
| 과제명 | **Zone2Runner** — 개인화 유산소(Zone 2) 러닝 코칭 앱. "당신의 Zone 2를 찾아 지켜주는 러너의 동반자"라는 의미 |
| 목표 | 갤럭시 워치의 심박을 실시간으로 보며, **개인에게 맞춰진 Zone 2 경계** 기준으로 판정하고, 온디바이스 AI가 **근거 있는 음성 코칭**과 **설명 가능한 리포트**를 제공한다 |
| 구성 | Android 폰 앱(판정/분석/개인화/코칭의 주체) + Wear OS 워치 앱(측정/표시) — 자체 서버 없음. 심박·개인화·세션 분석은 자체 서버나 외부 AI API로 보내지 않으며, 날씨와 지도는 외부 서비스를 사용 |
| AI 적용 | 판정=규칙, 개인화=온라인 베이지안(말하기 테스트 실라벨), 분석=관측 통계, 표현=온디바이스 LLM(가드 3종 통제) |
| AI 적용에 따른 예상 효과 | ① 사용자 응답을 받을수록 개인 경계를 조정한다 ② 판정과 코칭의 근거를 다시 확인할 수 있다 ③ 자체 서버·유료 추론 API 비용이 없다 ④ 참값을 직접 측정하기 어려운 문제를 가상 러너로 반복 검증할 수 있다 |

---

## 슬라이드 3 — 기능 요구사항 및 제약사항

### 기능 요구사항 (AI 관련 = FR2~FR6)

| ID | TITLE | Description | AI |
|:--:|:--|:--|:--:|
| FR1 | 개인 프로필 관리 및 Zone 2 초기 경계 산정 | 사용자의 신체 정보(나이·키·체중·안정심박·최대심박)를 프로필로 관리하고, 첫 러닝에 쓸 Zone 2 경계를 계산한다. 최대심박은 Tanaka 공식으로 추정하고, 안정심박을 알면 Karvonen 방식으로, 모르면 최대심박 비율(%HRmax)로 경계를 잡는다. | |
| FR2 | 실시간 심박 수집 및 존 판정 | 1초마다 심박을 받아 규칙으로 현재 존을 판정한다. 손목 심박은 노이즈가 많으므로, 생리 범위를 벗어난 값을 걸러내고(입력 검사), 경계 근처에서 판정이 깜빡이지 않도록 여유 구간(히스테리시스: 한 번 넘으면 조금 더 내려와야 되돌아옴)을 두며, 신호가 끊기면 마지막 값으로 잠시 버틴다. 판정 기준은 둘이다 — 화면 표시는 순간 심박(5개 존), 코칭·통계는 60초 지속 심박. | ● |
| FR3 | 관측 데이터 분석 엔진 | 미래를 예측하지 않고, 관측한 데이터만으로 파생 지표를 계산한다 — 심박 드리프트, 같은 페이스 대비 심박(서브맥시멀), 운동 후 심박 회복(HRR), 경사 보정 페이스, 케이던스 안정성. 예측값은 맞았는지 확인할 방법이 없어 검증이 불가능하므로 도입하지 않았다. | ● |
| FR4 | 개인화 적응 | 러닝이 쌓일수록 경계를 그 사람에게 맞춰 간다. 최대심박은 더 높은 값이 관측되면 위로만, 안정심박은 더 낮은 값이 관측되면 아래로만 갱신하고, Zone 2 상한은 러닝 중 사용자가 답하는 말하기 테스트(실제 라벨)를 근거로 온라인 베이지안으로 조정한다. 공식이 준 초기값에서 시작해 그 사람의 실제 반응 지점으로 수렴한다. | ● |
| FR5 | 반응형 실시간 코칭 및 안전 가드 | 존을 벗어나거나 심박이 흐르는 방향에 맞춰 실시간으로 코칭한다. 코칭의 방향과 사실은 규칙·분석 엔진이 확정하고, 문장은 LLM이 만든다. 나가는 문장은 형식·방향·숫자 검사를 거치며, 검사에 걸리거나 LLM이 없으면 짧은 기본 문구로 끊김 없이 대체한다. 위험할 만큼 심박이 높으면 LLM을 거치지 않고 규칙이 즉시 권고한다. | ● |
| FR6 | 세션 리포트 및 근거 설명 | 러닝이 끝나면 파생 지표를 그래프와 카드로 보여주고 "왜 이렇게 판정하고 코칭했는지"를 설명한다. 세션 이야기와 각 코칭 문장의 근거를 눌러서 추적할 수 있으며, LLM이 만드는 설명 문장은 종료 시 한 번만 생성해 저장한다(다시 열어도 재생성하지 않는다). | ● |

- 규정 충족: FR 6개(최소 4개 이상), AI 관련 FR 5개.

### 제약사항

| ID | TITLE | Description |
|:--:|:--|:--|
| C01 | 운영 비용 0 | 자체 서버나 유료 추론 API를 쓸 수 없다. 모든 추론과 LLM 실행은 사용자의 기기 안에서 이루어져야 한다. |
| C02 | 대량 학습 데이터 확보 불가 | 이 도메인(개인 러닝 심박)에 맞는 대량의 학습 데이터를 확보할 수 없다. 공개 데이터셋은 여러 사람의 평균이라 개인의 차이를 담지 못한다. |
| C03 | 개인 참값 측정 불가 | 개인의 유산소 역치(젖산·환기역치) 참값을 실측할 수 없다. 실험실 장비 없이는 정확한 정답값을 얻을 수 없다. |

---

## 슬라이드 4 — 품질 요구사항 (QAW / Utility Tree → ASR 선정)

강의의 AI 8대 품질 중 이 프로젝트의 문제와 구현에 직접 연결되는 네 가지를 골랐다. 네 항목 모두 자동시험으로 합격 여부를 다시 확인할 수 있다. 이로써 규정인 QA 4개 이상, AI 품질 2개 이상을 충족한다.

| ID | AI QA | 왜 필요한가 | QAS와 합격 기준 요약 | 중요도 | 난이도 |
|:--:|---|---|---|:--:|:--:|
| QA1 | **설명용이성** | 참값을 직접 측정하기 어려우므로 판단에 사용한 사실을 확인할 수 있어야 한다. | LLM 문장에 근거에 없는 숫자가 들어오면 차단한다. 검증 집합에서 미등록 숫자 5/5 차단, 사실 문장 3/3 통과, 근거 기록 시험 통과 | H | M |
| QA2 | **기능적응성** | 평균 공식값을 그대로 쓰지 않고 사용자 응답으로 개인 경계를 바꿔야 한다. | 말하기 테스트 응답으로 경계를 갱신한다. 15개 가상 사례에서 최종 오차 3bpm 이하, 5세션 안착, 마지막 4세션 폭 4bpm 이하, 세션 이동 10bpm 이하 | H | H |
| QA3 | **제어가능성** | 생성 문장이 판정 권한을 가져서는 안 된다. | 판정과 어긋난 LLM 문장을 규칙이 막고 기본 문구로 바꾼다. 검증 집합에서 위반 10/10 차단, 정상 5/5 통과, 실패 경로 시험 통과 | H | M |
| QA4 | **강건성** | 센서값이 흔들리면 판정과 개인화도 흔들린다. | 범위 밖 값을 버리고 경계 부근 번복을 줄인다. 이상치 50/50 차단, 정상 오차단 0/500, 번복 60% 이상 감소, 실제 이동 2틱 안에 반영 | M | L |

- **핵심 ASR**: QA2 기능적응성, QA1 설명용이성, QA3 제어가능성
- **지원 QA**: QA4 강건성
- 테스트가능성은 네 QA를 자동 검증하게 한 공통 설계 수단으로, 수행효율성은 실기기 확인이 필요한 보조 제약으로 관리한다.

**Utility Tree (요약)**:

```
유용성(Utility)
├─ [핵심] 기능적응성 ── QA2 사용자 응답 → 개인 경계 갱신 → 안정       (H, H)
├─ [핵심] 설명용이성 ── QA1 판단에 사용한 사실에서 벗어나지 않음       (H, M)
├─ [핵심] 제어가능성 ── QA3 어긋난 LLM 출력 차단 → 기본 문구          (H, M)
└─ [지원] 강건성 ───── QA4 이상값 차단 + 경계 부근 판정 번복 감소      (M, L)
```

**QA → 설계 → 검증 연결**:

| QA | 담보하는 설계 | 검증 |
|---|---|---|
| QA1 설명용이성 | DP1 (투명 판정 + 프로비넌스) | 검증 표 QA1 |
| QA2 기능적응성 | DP3 (온라인 베이지안 적응) | 검증 표 QA2 |
| QA3 제어가능성 | DP2 (구조적 격리 + 가드 3종) | 검증 표 QA3 |
| QA4 강건성 | DP4 (계층 방어 판정) | 검증 표 QA4 |

RunSource와 가상 러너는 위 네 항목을 워치 없이 반복 검증하게 한다. 이 구조는 DP5에서 설명하되 별도 AI QA로 세지 않는다.

- **선택의 핵심**: 대량 데이터와 개인 참값이 없으므로, 복잡한 모델의 정확도를 주장하는 대신 개인화 과정과 판단 근거와 AI 통제와 입력 방어를 설계했다.
- **의도적으로 뺀 AI 품질**: 기능정확성은 실제 개인 참값을 측정할 수 없어 제외했다. 공정성은 집단 비교 대상이 없는 1인 개인화 과제라 제외했다. 수행효율성은 현재 JVM 로직만 측정해 보조 제약으로 내렸다. 프라이버시는 심박·개인화 데이터의 외부 AI 전송이 없다는 경계 사실로 설명하되, 날씨·지도 외부 요청까지 없다고 주장하지 않는다.
- QAS 6요소와 합격 기준은 spec/spec-002-quality-attributes.md, 실제 결과와 한계는 report/report-015-qa-verification.md가 정본이다.
