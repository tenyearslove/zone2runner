# Zone2Runner 문서 작업 지침

이 저장소는 회사의 **AI Specialist(설계) 인증**을 위한 1인 과제다. 상용 제품 완성도를 과장하는 것이 아니라, 제약과 품질 요구에서 아키텍처 결정을 도출하고 구현과 검증으로 추적하는 **AI S/W 설계 역량**을 보여주는 것이 최우선 목표다.

## 작업 범위

- 사용자가 별도로 요청하지 않는 한 `app/`, `wear/` 소스코드는 수정하지 않는다.
- 주 작업은 문서 논리 재구성, 자연스러운 한국어 교정, 인증 보고서, 발표 대본, 근거 추적이다.
- 기존 문구를 그대로 신뢰하지 말고 구현, 테스트, 측정 산출물과 대조한다.

## 정본 우선순위

1. 실제 동작: `app/`, `wear/`
2. 요구사항과 QA 정의: `spec/spec-001-requirements.md`, `spec/spec-002-quality-attributes.md`
3. 아키텍처: `arch/architecture-overview.md`, `arch/component-catalog.md`, 활성 ADR, `arch/dp/`
4. 보고서 원고: `report/`
5. 화면용 HTML: `docs/src/` (보고서 원고의 표현물, 독립 정본으로 취급하지 않음)
6. 과거 기록: 각 `archive/`, `HANDOFF.md`

문서와 구현이 충돌하면 구현 사실을 기준으로 충돌을 기록하고, 보고서 문구를 고친다. 정의를 바꿔야 할 정도의 충돌이면 먼저 사용자에게 알린다.

## 서술 원칙

- 흐름은 `문제/제약 -> 품질 시나리오 -> 대안 -> Trade-off -> 결정 -> 구현 -> 측정 -> 한계`를 따른다.
- "AI를 썼다"보다 **AI의 권한과 실패를 어떻게 설계했는지**를 보여준다.
- 시뮬레이션/단위 테스트는 구조와 메커니즘 검증으로만 표현한다. 실제 사람의 생리 정확도나 실기기 성능으로 일반화하지 않는다.
- 측정하지 않은 수치, 사용자 평가, 의료적 효과, 상용 운영 효과는 만들지 않는다.
- 완료/실측/계획을 명확히 구분한다. 재현하지 못한 과거 측정은 실행 날짜와 재현 조건을 함께 적는다.
- 자연스러운 한국어 문장을 우선한다. 불필요한 영문 병기, 구호, 반복되는 "정체성" 표현, 과도한 별점은 줄인다.
- 운동 코칭은 참고용임을 유지하고 진단, 치료, 안전 보장을 주장하지 않는다.

## 핵심 프로젝트 논지

Zone2Runner의 설계 과제는 "정답이 없는 개인 운동 강도 문제에서, 제한된 온디바이스 모델을 사용하면서도 판단 근거, 사용자 교정, 실패 시 폴백, 재현 가능한 검증을 갖춘 코칭 시스템을 어떻게 설계할 것인가"이다.

핵심 선택은 다음과 같다.

- 판정과 안전 권고는 투명한 규칙/통계가 담당하고 LLM은 표현만 담당한다.
- 개인 경계는 공식 초기값에서 시작해 말하기 테스트 라벨로 온라인 적응한다.
- 센서 노이즈는 입력 검사, 히스테리시스, 시간창, 폴백으로 계층 방어한다.
- 센서 소스를 추상화하고 가상 러너를 사용해 같은 자극을 재현한다.

세부 리뷰와 작업 계획은 `docs/review/2026-08-04-codex-transition-review.md`를 따른다.

## 로컬 도구 경로

- Android SDK: `D:\Android\Sdk`
- Poppler: `D:\Applications\poppler-26.02.0\Library\bin`
- PlantUML JAR: `D:\Applications\plantuml\plantuml-1.2026.6.jar`
- PlantUML native: `D:\Applications\plantuml\native-plantuml-windows-amd64-1.2026.6\plantuml.exe`
- Android 테스트 실행 시 현재 셸에 `ANDROID_HOME`과 `ANDROID_SDK_ROOT`를 모두 위 SDK 경로로 지정한다.

## 다이어그램 규칙

- 새 아키텍처 그림은 반드시 PlantUML `.puml`을 정본으로 작성하고 PNG로 렌더링한다.
- 기존 그림을 수정할 때도 PNG를 직접 편집하지 않고 대응하는 `.puml`을 수정한 뒤 다시 렌더링한다.
- 일반 아키텍처 도식은 `arch/diagrams/`, DP 전용 도식은 `arch/dp/dp-*/images/`에 `.puml`과 `.png`를 함께 둔다.
- Windows 렌더는 UTF-8, `Malgun Gothic`, Smetana 레이아웃을 기본으로 한다. 보고서 반영 전 PNG의 한글, 화살표, 잘림, 글자 크기를 확인한다.

## Git 작업 규칙

- 문서 작업도 검증 가능한 논리 단위가 끝날 때마다 커밋하고 현재 upstream으로 푸시한다.
- 커밋 단위 예: 리뷰 기준 확정, 주장-근거 대장, 보고서 파트 재작성, PUML+PNG 갱신.
- 관련 없는 사용자 변경은 커밋에 포함하지 않는다.
- 생성 산출물은 원본과 함께 검증한 뒤 커밋한다. PlantUML 변경은 `.puml`과 렌더된 `.png`를 같은 커밋에 포함한다.

## 협업과 피드백 원칙

- 사용자 피드백을 지시사항처럼 기계적으로 반영하지 않는다. 먼저 사실, 과제 목적, 문서 전체의 일관성과 대조해 평가한다.
- 피드백마다 `동의`, `부분 동의`, `다른 제안`을 구분하고, 중요한 판단에는 이유와 예상 영향을 함께 설명한다.
- 사용자의 의견과 다른 결론이면 근거를 분명하게 제시하고 토론한다. 다만 최종 선택은 사용자가 판단할 수 있도록 선택지와 trade-off를 쉬운 말로 제공한다.
- QAS 선정, 측정 지표, DP 유지·삭제처럼 문서 전체에 영향을 주는 결정은 합의하기 전에 일괄 수정하지 않는다.
- 표현 교정처럼 영향이 작고 되돌리기 쉬운 변경은 진행하되, 의미나 주장 수준이 달라지는 변경은 먼저 논의를 거친다.
